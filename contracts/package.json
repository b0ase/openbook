{
  "name": "openbook-contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Pay-to-mint covenant for $Ticker supply. Testnet only.",
  "scripts": {
    "compile": "npx scrypt-cli compile",
    "test": "npx scrypt-cli compile && mocha",
    "genkey": "node -r ts-node/register scripts/genkey.ts",
    "deploy": "node -r ts-node/register scripts/deploy.ts",
    "mint": "node -r ts-node/register scripts/mint.ts"
  },
  "dependencies": {
    "scrypt-ord": "^1.0.17",
    "scrypt-ts": "^1.4.5"
  },
  "devDependencies": {
    "@types/chai": "^4.3.20",
    "@types/mocha": "^10.0.10",
    "@types/node": "^26.2.0",
    "chai": "^4.5.0",
    "mocha": "^11.8.0",
    "scrypt-ts-transpiler": "^1.2.29",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}