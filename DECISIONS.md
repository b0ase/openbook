# Decisions

> Key decisions already made. Don't relitigate these unless the reasoning no longer applies. If you're an AI, respect these — they came from deliberate discussion, not defaults.

## Naming

- **Project name:** OpenCook (formerly "Build From Nothing" → "BSVibes" — renamed 2026-03-23; rebranded to OpenCook 2026-06-13)
- **Subtitle:** Agentic Fairness — fairness enforced by autonomous AI agents, not committees
- **bOpen.ai** is the toolkit, not the product. "created with bopen.ai" shown in UI
- **User-facing language:** Use crypto terms only when they're the clearest option, always in a friendly context. No jargon for jargon's sake, but don't avoid words users already understand.
  - "key" and "recovery key" are fine — Google/Apple have normalised these
  - "WIF", "private key", "ECDSA", "pubkey" — too technical for UI, avoid
  - "wallet" — avoid (OpenCook isn't a wallet app)
  - "address" — use sparingly, prefer "deposit" or "add funds" where possible
  - "passphrase" over "PIN" (minimum 8 chars, not a 4-digit PIN)
  - When in doubt: would a non-crypto person understand this without Googling? If not, rewrite it

## Identity & Security

> ⚠️ **SUPERSEDED (2026-06-14) — read first.** Key rotation / the migration system was **REMOVED** in favour of encrypt-in-place: the key/address **never changes**. So any entry in this section referencing key **rotation**, **migration chains**, `migrateIdentity` / `upgradeIdentity` / `commitUpgrade` / `cleanupMigrations`, the deleted `MoveAddressModal` / `StaleKeyModal`, `/api/restore-eligibility`, or recovery files carrying an `oldWif` / a `"rotation"` / `"pre-rotation"` pathType is **HISTORICAL** — kept as an honest decision record, not live spec. Current shape: `encryptInPlace` / `changePassphrase` re-wrap the SAME WIF; recovery files are single-key (`pathType: "save" | "restore-pre"` only, `opencook-` filename prefix). See "Key rotation REMOVED in favor of encrypt-in-place" further down.

### Current state (Phase 1 — acceptable for now)
- BSV keypair generated in-browser via `PrivateKey.fromRandom()`
- Stored as plaintext WIF in localStorage
- No encryption, no password, no PIN
- Acceptable only because no real money is at stake yet

### The hardware problem (settled)
- BSV uses secp256k1 elliptic curve
- No hardware chip supports secp256k1 directly (not Apple Secure Enclave, not TPMs, not passkeys, not YubiKeys)
- Hardware can't **be** the BSV key, but hardware can **guard** it via encryption wrapping

### Planned upgrade path (6 stages, settled)
1. **Fix Now:** Server-side signature verification, rate limiting, try/catch on JSON.parse, hide WIF from DOM, CSP headers
2. **Stage 1 (current):** Raw localStorage with renamed labels
3. **Stage 2:** Passphrase encryption — download backup first, then set passphrase, AES-256 encrypt localStorage. Fresh key at upgrade time
4. **Stage 3:** Passkey wrapping — WebAuthn PRF replaces passphrase. Firefox falls back to passphrase. HKDF domain separation
5. **Stage 4:** Self-funded posting — UTXO check, client-side tx building, server fallback
6. **Stage 5:** Revenue + daily limits — 5 free posts/day, QR to fund, fairness agent routes revenue
7. **Stage 6:** Server HSM / threshold signing — required before significant funds flow

> **[PARTIALLY SUPERSEDED 2026-06-13 — see "Key rotation REMOVED in favor of encrypt-in-place" later in this file.]** The "5-minute window problem" and "Key rotation on upgrade" subsections below describe the ROTATION-based design (`MoveAddressModal`, a NEW key on upgrade, an on-chain migration chain) — all REMOVED. **Current model:** the key/address NEVER changes; adding a passphrase encrypts the EXISTING key in place (`encryptInPlace`) — no rotation, no migration, no `MoveAddressModal`. The plaintext-window risk is now mitigated by gating recovery-file export behind setting a passphrase (no unencrypted file ever leaves the device), not by rotation. (Passkey-wrapping, further below, REMAINS a valid future enhancement — it wraps the same key.) These subsections are retained as historical record of the original design.

### The 5-minute window problem (settled + implemented)
- Any key that existed as plaintext in localStorage must be assumed potentially compromised
- **Implemented:** Protection happens via `MoveAddressModal` (the "Passphrase" row of the You modal, also the entry point from the "Not protected" red banner). The wizard generates a NEW key, the old key signs a migration, and the new key is stored AES-256-encrypted.
- Migration posted on-chain via OP_RETURN — permanent verifiable link from old pubkey to new pubkey
- All contribution history and future payments follow the migration chain
- Old key becomes useless after migration — someone stealing it gains nothing

### Security upgrade model (settled)
- **Self-service:** Unprotected users see a red "Not protected" banner at the top of the identity dropdown that opens `MoveAddressModal` directly. Protected users use the same wizard via the "Passphrase" row in the You modal.
- **Deferred activation (future):** System nudges users when earnings reach a threshold (e.g., $5) but haven't upgraded yet
- **Zero friction for new users:** No passphrase required on first visit. Upgrade is optional and user-initiated
- **Key rotation on upgrade:** Fresh keypair born encrypted, never existed as plaintext

### Passkey-wrapped keys (chosen approach)
- BSV key encrypted with AES-256
- Decryption key derived from WebAuthn PRF extension tied to biometrics
- Stolen localStorage = useless ciphertext
- Works on Chrome, Safari, Edge; Firefox needs passphrase fallback
- Medium implementation effort, best security/UX tradeoff

## Self-Funded Posting (settled)

- Server pays for posts by default (~0.00001 BSV per post)
- When user has BSV balance, app silently switches to user-funded
- Same button, same UX — funding source switches invisibly
- Must create change output or user loses remaining balance
- Cost: ~1 satoshi per post; 10,000 satoshis covers thousands of posts

## Anti-Spam (settled direction)

- Free posts capped per day (5/day suggested)
- Under limit: server pays, no friction
- Over limit with balance: self-funded, no friction
- Over limit without balance: "You've got more to say" + QR code
- Server-side enforcement (pubkey + IP + session token), not chain-only
- Optional: proof-of-work for free posts

## Launch generous, tighten with scale (settled 2026-07-20)

- **Strategy:** launch with deliberately generous economics — free posting, every post earns a pool share, free boosts — to bootstrap the network (the classic marketplace "free to start" playbook). Tighten as the platform grows and there is a network worth protecting + users who can carry their own cost.
- **Known risk knowingly accepted:** free-post weight-farming — post count adds pool weight *linearly* (`sqrt` dampens boosts-per-post, not post count) and posting is free, so spamming free posts inflates pool share cheaply. Bound + detail in SECURITY_AUDIT.md **L8** (negligible at launch ≈ cents/day, ~$700–1k/yr worst-case at scale; harm is fairness-dilution/reputation, not theft).
- **Why deferred, not fixed now:** the cheap per-identity weight cap is sharding-bypassable (fresh keys are free), so it isn't worth building. The effective levers are structural — engagement-gated payout weight (pool earnings follow demonstrated engagement, not raw posting) and/or posting costs / reduced subsidies — tightening moves best applied *at scale*, when they're both needed and users can carry the cost. These are directional levers, not committed features.
- **Tripwire (so "later" isn't open-ended):** revisit when daily paid-boost volume exceeds ~$50/day, OR any single identity/IP cluster holds >20% of total pool weight, OR the vector is publicly demonstrated.
- **Safe to defer:** runtime weight-formula change only (no on-chain format or DB migration; `boot_split` carries `formula_version` so the split rule can change forward), addable any time. Sole caveat: on-chain payouts can't be clawed back — hence the tripwire.
- **Don't relitigate pre-scale.**

## Launch pool cutoff — fresh pool at launch via `launchTs` (implemented 2026-08-02, money-path auditor: SHIP)

- **Decision:** the earnings pool has an EPOCH. Posts with `created_at < launchTs` (the backdated genesis seed + all pre-launch/test posts) are EXCLUDED from both the 80% contributor pool (`weights.ts` `calculateWeights`) and the dynamic boot-price contributor count (`pricing.ts` `countActiveContributors`). Posts at/after `launchTs` earn normally. **This is PERMANENT** — not a temporary flag (see memory `project_opencook_genesis_seed`, agent-verified keep-forever: removing it later reclaims ~10% of the pool at 3mo, is negligible at 12mo, and is a second money-path change for no gain).
- **Pre-launch posts STILL earn the 15% creator bonus** when boosted — that bonus is pool-independent (`split.ts` pays it to the boosted post's own address, ungated). So "genesis posts don't draw from the pool but still get paid for boosts" is satisfied with zero extra code.
- **Mechanism:** one config value `launchTs` (`config.ts`), env-overridable via `LAUNCH_TS`, added as `AND created_at >= ?` to the two money queries only. `created_at` is SQLite space-format UTC `"YYYY-MM-DD HH:MM:SS"` (BINARY collation → lexicographic == chronological), so the string compare is exact; `>=` means a post at the exact launch instant is post-launch. **`LAUNCH_TS` MUST be set in UTC** at deploy (a local-time value silently shifts the epoch) — blocking `LAUNCH_CHECKLIST` step.
- **Fail-closed fallback:** if `LAUNCH_TS` is unset, `launchTs` defaults to a far-future sentinel `"2999-01-01 00:00:00"` → the pool stays EMPTY (floor price 1000), never leaking a pre-launch post into a real payout. Erring late is safe; erring early (a real date guessed before an actual slipped launch) would leak — hence the sentinel, not a date guess.
- **Supersedes** the earlier `pool_excluded` pubkey-table idea from the genesis plan: a `created_at` epoch is cleaner and, unlike a pubkey ban, lets the 3 genesis users earn normally from their FUTURE post-launch posts.
- **Threaded as an optional param** (`launchTs = FAIRNESS_CONFIG.launchTs`) for testability; both prod callers pass `(db)` only. Non-blocking note: the 30s weights cache isn't keyed on `launchTs` — safe because it's a deploy-constant; a footgun only if a future caller varies it per-request. Tests: 199 total (5 new cutoff cases proving pre-launch=0, post-launch earns, `>=` boundary inclusion, mixed-pubkey counts only post-launch).

## Quiet launch: noindex, NO password gate (decided 2026-08-10, two-agent reviewed)

- **Decision:** the pre-public "quiet launch" phase uses an **env-driven `noindex`**, NOT an HTTP Basic-Auth password gate. Search-indexing is OFF by default (`src/app/robots.ts` serves `Disallow: /`; `layout.tsx` emits a `noindex, nofollow` meta) and flips ON only when `ALLOW_INDEXING=true` (set at go-public). The site is otherwise **openly reachable** at `opencook.fun` — unadvertised, shared directly with a small trusted group.
- **Why no password gate** (a Basic-Auth `middleware.ts` was designed, then dropped): the two risks a gate addresses are already covered or cheap to cover otherwise — (1) **wallet drain** is bounded by the existing per-IP free-boost caps + ~$0.20/day server-spend ceiling + kill-switch, so an open site can't be drained; (2) **search-engine first-impression** is handled by `noindex` with zero friction. Against that, the browser's Basic-Auth popup is real friction for non-technical invitees (asks for a username we ignore, awkward on mobile). The only residual risk — a stranger's **permanent on-chain post** landing in the feed — is guarded by `CONTENT_DENYLIST` (illegal-floor) and mitigated by the option to re-upload the pristine genesis DB before going public. Owner chose the lower-friction path with eyes open.
- **Reverse at go-public:** set `ALLOW_INDEXING=true` (one env var, no code change) — `LAUNCH_CHECKLIST` Stage 4. Forgetting it leaves the public site un-indexed (invisible to Google), so it's a flagged Stage-4 step; default-noindex is the safe direction (never accidentally indexes a rough build).
- **`CONTENT_DENYLIST` also deferred to go-public (decided 2026-08-12):** the quiet launch went live with the illegal-floor filter EMPTY. Rationale: the small TRUSTED group is the mitigation, and an empty filter avoids over-blocking legal speech before it's tuned (a text denylist matches words not intent — an improvised list is both incomplete AND over-broad, which cuts against the free-speech ethos). **HARD pre-public blocker:** populate the illegal floor (primarily CSAM) from a lawyer / trust-and-safety-sourced keyword list (part of the ~1 hr CSAM/broadcaster legal review), NOT an improvised one. See `LAUNCH_CHECKLIST` §1.
- **Railway healthcheck stays `/`** (NOT `/api/health`): `/api/health` returns 503 on operational issues (low balance / no wallet) by design for the UptimeRobot monitor, which would fail Railway's deploy healthcheck. `/` is 200 whenever app + DB are up. (This mattered under the gate design too; recorded so it isn't re-broken.)

## Genesis DB seed-on-boot (built 2026-08-11, agent-verified: SHIP-with-fixes → fixed + re-verified)

- **How the genesis DB reaches production:** `seed/genesis.db` (824 KB, 2006 on-chain posts) is **committed to the repo**; `scripts/seed-if-empty.mjs` runs before `npm start` (Dockerfile CMD, `;` not `&&` so a seed hiccup can't block startup) and copies the seed into `DATABASE_PATH` ONLY when the target is missing or provably empty (opens cleanly, has a `posts` table, 0 rows). Railway volumes start empty, so this seeds the genesis feed on first boot and is a no-op idempotent **skip** on every later redeploy — live posts/earnings are never overwritten.
- **Fails toward PRESERVATION:** an existing DB that won't open, is malformed/corrupt, or errors unexpectedly is NEVER overwritten (only "no such table" or a clean 0-row count is treated as seedable). A general-purpose agent flagged the original `catch→seed` as a data-loss inversion; fixed. Verified with 4 local cases: missing→seed, empty→seed, has-posts→preserve, corrupt("file is not a database")→preserve.
- **Why committed, not off-repo** (reverses the earlier "keep the genesis DB off-repo" note): at 824 KB of already-public on-chain data, bundling + copy-on-boot is far more reliable than hand-uploading a binary to a Railway volume (Railway has no CLI volume-upload; the volume is only reachable inside the running container). Sidecars `seed/genesis.db-wal`/`-shm` are gitignored; if the seed is ever regenerated, run `PRAGMA wal_checkpoint(TRUNCATE)` first so nothing is stranded in an uncommitted WAL.

## Feed: LIVE/ORIGIN modes + upward infinite scroll (redesigned 2026-08-03, agent-designed + agent-implemented; preview-tested, device-QA pending)

- **Problem:** the feed is chat-style (newest at bottom). Reaching the true first post (the origin story matters for OpenCook) meant paging backward 100-at-a-time through ~2,000 seed posts, and the scroll-anchor compensation for prepending older posts kept fighting the Genesis jump / the static founding block → jank (yank on scroll-up, 2-click down button, flash-and-retreat).
- **Design (two agents + a fork implementation):** two modes on one feed.
  - **LIVE (default):** land on newest (bottom); a returning user with an in-window unread boundary lands on their first-unread post with an amber divider (`opencook_last_read_id` localStorage, frozen `firstUnreadId`; away > a window → land at newest, no divider — deliberately NOT restoring exact pixel position). Scroll UP auto-loads older (`getOlderPosts`) via a top sentinel (**1000px top rootMargin** so the fetch+prepend+anchor settle off-screen — the key iOS defense) with **bottom-relative scroll anchoring** (`el.scrollTop += el.scrollHeight − before`, captured synchronously pre-prepend, applied in a pre-paint layout effect keyed on `[olderPosts, liveHasMore]` — chat-app standard, robust on iOS vs. the fragile element-anchor we removed).
  - **ORIGIN (Genesis button, cross-fade):** loads the oldest window (`getOldestPosts`), founding block + true post #1 adjacent at top, scroll DOWN appends `getForwardPosts` (append = inherently jank-free). `handleGoLive` (↓) cross-fades back + resets the LIVE window.
- **Founding block gating (kills the yank at its root):** the static `<Genesis>` founding block renders at the LIVE top ONLY when `liveHasMore` is false (post #1 loaded) — mutually exclusive with the upward-load sentinel. So it never sits adjacent to a mid-history post, and the sentinel→Genesis reveal commit is itself anchor-compensated (why the anchor effect is keyed on `liveHasMore` too).
- **Perf without windowing:** the LIVE unread IntersectionObserver only observes posts in the newest window (`post.id >= oldestServerId`), so the observed set stays ~100–200 no matter how far up you scroll. ~2,000 plain `<article>` nodes is fine at launch → **no sliding-window trim for launch** (follow-up only if device-QA shows jank past ~800 nodes).
- **Jank causes explicitly closed:** duplicate-fetch → duplicate-key freeze (synchronous `olderLoadingRef` lock + dedupe Set); Genesis-reveal height jump (anchor keyed on `liveHasMore`); mount-time sentinel race (pre-paint landing wins + `landedRef` gate); poll-append contamination (anchor not keyed on `serverPosts`; `prependPrevHeightRef` null on append commits).
- **Cross-fade over "scroll-then-jump":** the owner's instinct was scroll-then-snap; adopted a ~200ms opacity cross-fade instead because scroll-then-snap fights iOS momentum/rubber-band (the exact fragility being removed).
- **Net:** deleted the fragile upward-scroll v1 + element-anchor; ended up LESS code than the janky version. **Preview-tested OK on the 2,118-post launch.db; the returning-user unread-line is unverified in preview (needs live new posts). Full device-QA pending (Phase 8).**

## Bootboard (settled)

- **Mechanic:** Any post can be "booted" to a spotlight slot by paying a fee. Someone else pays, you get booted off
- **Boot count:** Tracked per post — shows how many times a post has been featured
- **Revenue model:** Built into the UX, not bolted on. Creates natural urgency and competition
- **Animations:** Shake + glow + slide-in on holder change. Expandable history
- **Boot icon:** Uses 🥾 emoji (custom SVG attempted, reverted to emoji for clarity at small sizes)

## Agent Chat (settled)

- **AI-powered:** Uses Claude Haiku 4.5 via Anthropic API with streaming SSE responses
- **System prompt:** Single source of truth in `src/data/agent-prompt.ts`
- **Endpoint:** `/api/agent` route handler (POST, streams text chunks)
- **Cost:** ~$0.001 per question (~25,000 questions per $25 credits)
- **Rate limiting:** 30 requests/min per IP + max 3 concurrent requests (prevents Anthropic API overload)
- **Input limits:** Max 20 messages, 2000 chars each per request
- **Location:** "Ask AI" pill button below compose box, opens as centered modal (bottom sheet on mobile)
- **Post button:** Telegram-style — mic icon when empty, amber send arrow when text is present

## Genesis Section (settled)

- **Founding conversation** preserved at top of feed as immutable record
- **Visited state** persisted in localStorage — shows full "Genesis" pill first visit, discreet chevron after
- **Fairness agent tie-in:** This is the starting point for contribution tracking
- **NOT collapsible — by design.** Genesis is feed content, not a UI widget. It lives at the top of the scroll area. Users discover it by scrolling up (via the Genesis button), not by toggling a panel. Do not add a collapse/expand toggle.

## Feed UX Model (settled)

- **Telegram-style:** User enters at the most recent post (bottom of feed). Feed grows upward.
- **Unread tracking:** When user leaves and returns, new posts accumulate. Unread counter badge shows on the scroll-to-bottom button. IntersectionObserver marks posts as read when they scroll into view.
- **Navigation:** Scroll-to-bottom button (with unread count) and genesis chevron (scroll to top) are the two navigation anchors. Users explore the full history by scrolling between them.
- **Mobile enter-to-post:** The Telegram-style mic→arrow toggle on the compose button is the primary affordance. The "Enter to post" text hint is desktop-only — this is intentional, not a bug. Mobile users tap the amber arrow.
- **No collapse, no accordion, no "read more" gates** on any feed content. The feed is a continuous scroll.

## Agentic Fairness (settled direction)

- **Phase 1:** Human-defined parameters, AI executes (current target)
- **Phase 2:** AI suggests parameter changes, humans approve
- **Phase 3:** AI adjusts within bounds, humans can override
- **Phase 4:** Fully agentic, humans only intervene on disputes
- The name describes the vision, not just today's implementation
- **Revenue model:** Boot fees split directly to contributors via multi-output BSV transaction. See **FAIRNESS.md** for the full model, formula, parameters, and gaming analysis
- **This is a demo model** — simple post-count + engagement + recency. Will evolve as real value contributions emerge (code, design, community). The point is proving the mechanism works first

### Revenue Distribution Rules (settled)
- **True no-custody:** Every sat in = every sat out in the same transaction. No database balances, no pending payouts, no IOUs. Even 1-sat shares get a UTXO output.
- **Boots require signed identity:** Booter must have a pubkey. Prevents free boot abuse (can't fake a new identity to get more free boots). Unsigned users can post but not boot.
- **Only signed posts are boostable:** The creator bonus (15%) needs an address to pay. Unsigned posts can't be booted. Encourages identity adoption.
- **Zero pool recipients → creator gets 95%:** If nobody qualifies for the pool (e.g., only 1 contributor who is the creator), the 80% pool goes to the creator. 5% still goes to platform.
- **Dynamic pricing formula:** `boot_fee = max(1000, min(250000, active_contributors × 156))`. Active = posted in last 30 days, counted by pubkey only. Price cached 1 hour. Rationale: 156 ensures bottom-25% contributors clear meaningful payouts under Pareto distribution.
- **Free boots:** First 15 per pubkey, tracked in SQLite `boot_grants` table. Server wallet pays at the **floor price** (1,000 sats), not the dynamic price. After 15, user funds their address via QR code. See "Free boots pay floor only" decision below for rationale.
- **$50/month operator budget:** Covers subsidised boots, on-chain posting, hosting, API. Sustainable through ~200 users, then user-paid boots and 5% platform cut take over.
- **Trustless P2P payments:** Paid boots are built client-side — user's browser constructs the multi-output BSV transaction directly to every contributor. Server never touches user funds. Server only provides the contributor list. The transaction itself is the verifiable proof. Free boots use the server wallet (server is the payer, not custodian).
- **Auto-switch:** Free boots → server pays. User has BSV balance → client builds trustless tx. No balance → show fund address QR. One click, ~800ms, zero custody.
- **Currency display:** Default is dollars ("Noob Mode"), toggle to sats ("Goat Mode"). Persisted in localStorage. Applies to chip balance, activity feed, and boot prices. BSV price from WhatsOnChain API, cached.
- **Currency default is ALWAYS dollars — no protection-aware auto-flip (settled 2026-06-26).** Removed the auto-switch-to-sats-on-protect (and its one-time `GoatModeToast`): it fired on EVERY load for any protected user and the moment a user added a passphrase, dumping a mainstream user into sats — "looked terrible" and contradicted the dollars-default ethos. Sats (Goat Mode) is now **opt-in only** via the always-visible You-modal toggle; an explicit choice is still honored forever. Dropped `useCurrencyMode`'s protection-aware initializer + `setModeProgrammatically`, the `IdentityBar` auto-flip effect, and the `GoatModeToast`. **Do NOT re-add a protection-aware sats default** — there is no screen where sats is the better default for a mainstream user; the toggle covers the power-user case.
- **Deploy on Railway, NOT a self-managed VPS — closed-alpha-first (settled 2026-06-27).** For a solo, non-technical operator, Railway (the repo is already wired: `railway.toml` + `Dockerfile`, `/data` volume) is the right host over a raw VPS. **Decider: server-wallet-key safety** — `BSV_SERVER_WIF` holds real funds; on a VPS anyone with root SSH can read it (env file, process memory), on Railway it stays in the operator's own env vars and no third party ever sees it. Railway also removes the VPS maintenance burden (SSL cert auto-renewal, OS patching, nginx, crash-restarts) and provides the trusted `x-forwarded-for` that every per-IP cap depends on. The VPS's only real edges — flat cost at large scale, total control — don't apply at alpha/early-launch scale. **Sequence:** closed alpha at `alpha.opencook.fun` (light gate) → public at `opencook.fun` (point the apex at the SAME service so the data + funded wallet carry over, then remove the gate) AFTER the lawyer pass on the 3 hard clauses. Concrete steps + gotchas live in `LAUNCH_CHECKLIST.md` "Deployment approach". **Do NOT switch to a VPS without re-justifying the server-wallet-key exposure.** (If managed-platform philosophy ever matters, Render/Fly.io share the same reasoning — the point is "managed, key stays yours," not Railway specifically.) **Cost re-checked 2026-06-27** (a generic Vultr-vs-Railway report claimed Vultr "4–5× cheaper"): that headline is almost entirely **managed PostgreSQL (~$92/mo) + Redis (~$30/mo)** — OpenCook uses NEITHER (SQLite file via `better-sqlite3` + an in-memory rate limiter; `package.json` has no pg/redis). At OpenCook's real profile (one small Next.js service + a SQLite file, no managed add-ons) the delta is **~$5–15/mo**, not 4–5×. **Reconsider Vultr/Hetzner only when:** (a) the Railway bill consistently clears ~$50–100/mo, OR (b) OpenCook outgrows single-instance SQLite (then it needs a real DB + external rate-limit store anyway — that's when the managed-DB math finally applies; Turso/libSQL stays close to the current code), OR (c) there's real DevOps capacity to own patching/TLS/backups/proxy AND secure the wallet-key box. Migration later is ~a weekend (portable Node app) — **no lock-in penalty for staying on Railway now.**
- **Identity import cleans up migrations (~~settled~~ SUPERSEDED 2026-05-26 by E29).** Original rule: "when importing an old key, any migration records pointing away from that key are automatically deleted." This is NO LONGER the policy. Old keys with forward migrations are now BLOCKED from restore entirely — `cleanupMigrations` is no longer called by any UI path. See entry below: "Restore of rotated keys is blocked outright (E29, Design C-strict)."
- **Security upgrade forces backup download (settled).** Auto-downloads the new key backup BEFORE the upgrade is considered complete. User cannot lose access to upgraded key.
- **Free boots are one-time only (settled).** 15 free boots per identity, never reset. Once used, user pays from their earnings or funded balance. No monthly reset, no balance-based gating. The 15 boots are an onboarding gift — "try before you buy." After that, the economy takes over.
- **Free boots pay floor only (settled 2026-04-09).** The server wallet pays the boot price floor (1,000 sats) on free boots, regardless of the current dynamic price. Supersedes the prior "dynamic price" rule. **Rationale:** per-user subsidy cost is now bounded at ~15,690 sats (15 × ~1,046) forever, independent of platform scale or contributor count. At BSV $25 that is ~$0.004/user; at BSV $100, ~$0.016/user — safely within the $50/month operator budget across all realistic price ranges. Dust concerns are handled by the existing split model (`split.ts:49` — every non-zero share is paid, down to 1 sat), by the sqrt × decay weight curve which naturally concentrates value on top contributors, and by the fact that free boots are a symbolic onboarding gift, not a full economic event. The real economy happens on paid boots where dynamic pricing (`contributors × 156`) keeps pool shares at ~125 sats each. **Trade-off:** contributors earn less per free boot than per paid boot. This is intentional — it preserves the onboarding gift framing without scaling the server subsidy cost with platform success. **Alternatives rejected:** tapering free-boot count (violates the "one-time only" decision above, has Sybil attack surface via contributor count inflation, creates UX unfairness between launch-day and later users, and breaks Phase 2 agent governance by making `freeBootsPerUser` non-constant), batching sub-dust payouts (breaks the trustless no-custody model), and top-K concentration (unnecessary — the sqrt curve already concentrates naturally). A GorillaPool miner fee deal is pursued separately as an optional optimization, not a dependency.

## Tech Stack (settled)

- Next.js 16 + TypeScript + Tailwind v4 + SQLite + BSV
- Telegram/X/GPT hybrid UI — feed-first, dark theme, pinned compose
- Server components by default, client only when needed
- Dynamic imports for @bsv/sdk
- **Linter:** Biome (replaced ESLint 2026-03-25 — ESLint script was broken, Biome is faster and simpler)
- **React Compiler:** Enabled (auto-memoization, free perf wins with React 19)
- **Identity:** Shared via IdentityProvider context (replaces 4 independent useIdentity() calls that each loaded BSV SDK)
- **ISR:** 10-second background revalidation on page.tsx (other users see new posts without manual refresh)
- **bootPost:** Wrapped in SQLite transaction with input validation (prevents race conditions on concurrent boots)
- **Foreign keys:** PRAGMA foreign_keys = ON (was decorative before)
- **Real-time:** Client polls /api/posts every 5s with since_id (pauses when tab hidden). Exposes `refresh()` for on-demand polling after post/boot
- **Optimistic UI:** Posts appear instantly at full opacity (no spinner — server confirms in ~50ms). Pruned on next poll (500ms early poll after post). Boot count increments optimistically (local +1), reconciled to authoritative on the next live-count poll (see below).
- **Live boot counts (settled 2026-06-23, Phase 8 QA #4).** The 5s `/api/posts` poll carries an additive `counts` param = the ids of CONFIRMED visible posts (capped 100); the server returns a lightweight `{id, boot_count}[]` (`getPostCounts`, only id+count — NOT full post bodies) folded into the existing `Promise.all`. The client merges these into the same atomic `setPosts` as the `tx_id` patch. **Why:** a post permanently leaves the `pending_tx` refresh set once it gains a `tx_id`, so its boot_count froze at load time — a boot from the Bootboard / another user / a server-funded boost never updated the display until reload. This makes counts authoritative-live from ALL sources. **Reconciliation:** `BootButton` resets its optimistic `+1` when the incoming `bootCount` prop advances (the authoritative count already includes the user's own boot), preventing a one-tick double-count. Purely additive — the `pending_tx`/`since_id` contract is unchanged, the `sweepOrphans()` ambient trigger is untouched, and `/api/posts` stays unrate-limited by design. Cost is trivial (~3KB/poll, a PK-indexed `WHERE id IN (...)`). Superset fix (also resolves the Bootboard-reboot-count case).
- **Keyboard pushes header/bootboard off-screen on iPhone (#6) — ROOT-CAUSED; both Option B (JS) and Option C (CSS `fixed`) tried + REVERTED; WON'T FIX, accepted as a known cosmetic quirk (2026-06-25).** On iPhone Safari, opening the soft keyboard scrolls the Header (logo + Origin nav + identity chip) and the Bootboard out of view. **Root cause (keep — still valid):** `layout.tsx` sets `interactiveWidget: "resizes-content"`, which shrinks the layout viewport to the space above the keyboard; the Feed shell (`h-[100dvh]`, top-anchored, compose box as its bottom child) then gets translated UP by iOS to surface the input, dragging the header/bootboard off the top. The `100dvh` shell came from commit `6c56093` (2026-05-13) as an iOS Safari URL-bar-clipping fix — must NOT be regressed. **Archaeology:** owner's hunch (Android / terms-privacy) was wrong; it was the iOS URL-bar fix. The modal `svh` decision (2026-06-03 below) is modal-scoped, separate. **Option B (commit `06f6842`, REVERTED):** a `visualViewport` resize/scroll listener wrote `--app-height`/`--app-vv-top` onto `<html>` and the `page.tsx` wrapper + `Feed.tsx` shell tracked it. It DID fix the push-off, but **REVERTED on owner iOS-Safari testing** for two reasons: (1) **multi-second lag** opening AND collapsing the keyboard — iOS fires `visualViewport.resize` LATE (only after the keyboard animation settles), so the JS-driven height snapped sluggishly instead of following iOS's smooth native `dvh` resize; (2) the `paddingBottom: calc(1rem + env(safe-area-inset-bottom))` footer addition made the compose box sit too HIGH above the keyboard (the inset does NOT collapse to 0 with the keyboard open on iOS, contrary to the plan's assumption). **DO NOT re-attempt the JS `visualViewport` height-override** — fighting iOS's native resize is the lag source. **NEXT (agent-designed):** a CSS-only / minimal-JS approach — e.g. anchoring the Header + Bootboard so they stay put when the keyboard opens — that does NOT JS-resize the shell and so can't lag. Keep `interactiveWidget: "resizes-content"` (native resize is smooth) and the iOS scroll-warmup hack. The actual mechanism (confirmed by a 3rd agent): iOS **focus-reveal scrolls the root/layout viewport** to surface the bottom-anchored text box, dragging top-of-flow content off; `overflow:hidden` can't stop a viewport-layer reveal-scroll. **Option C (commit `eebf6c1`, REVERTED):** Header + Bootboard moved into a `position: fixed top-0 z-40 bg-black` stack + a `ResizeObserver`-measured spacer, with NO keyboard-event JS. On owner iOS-Safari testing it was **smooth (no lag) but did NOT keep the header in view** — `position:fixed` pins to the *layout* viewport, but the bug is that iOS offsets the *visual* viewport (scrolls the page up by `visualViewport.offsetTop`), and fixed-positioning can't compensate for that offset. **CONCLUSION — WON'T FIX, accept the bug.** The two approaches fail in OPPOSITE ways and together prove there is no clean fix: keeping the header in view REQUIRES compensating for iOS's visual-viewport scroll offset, which ONLY JS can read (`visualViewport.offsetTop`) — and that JS is exactly what lags (iOS fires the events late). CSS can't read the offset, so the lag-free path can't fix it. No third option exists on iOS Safari for this bottom-anchored-compose layout. The header/bootboard scrolling off the top *while actively typing* is accepted as a minor cosmetic quirk (everything still works; they return on keyboard close). **DO NOT** re-attempt either the `visualViewport`-JS override (lags) or `position:fixed` (doesn't compensate for the offset). Restored: `Feed.tsx` to the simple flat shell (`h-[100dvh]`, header + bootboard in flow, footer `pb-4`).
- **Mobile-keyboard architecture reassessment + compose dock-to-keyboard (settled 2026-06-25).** After repeatedly hitting walls on the keyboard, the owner asked whether to RE-ARCHITECT the shell. Architecture-reviewer verdict: the problem splits in two. **(a) "Header/Bootboard stay visible when the keyboard opens" is a HARD iOS Safari platform limit** — the visible-viewport offset is readable only via `visualViewport.offsetTop` (JS, inherently late → lag); CSS can't read it. A pincer proof (the two reverted #6 fixes fail in opposite ways), no third primitive. No front-end re-architecture removes it; only a native shell (Capacitor) could, which is disproportionate for a cosmetic quirk that's milder in standalone PWA mode and that no web chat app solves on iOS. → **ACCEPT** (= #6 WON'T FIX above). **DO NOT pursue a reverse-scroll re-architecture or a native wrapper without re-raising** — both collide with settled feed-scroll + identity-storage decisions for a cosmetic gain. **(b) "Dock the compose bar onto the keyboard" IS fixable** and the CSS `:has()`/`:focus-within` collapse is the right, lag-free approach (kept, not reverted). **The dock (touch only):** on `pointer-coarse`, focusing the compose textarea collapses the Ask-AI/bookmark grid (`PostForm`) + attribution row (`Feed`) + tightens the footer padding, so the input drops onto the keyboard (was floating ~65px above). Pure CSS, NO `visualViewport`/JS → cannot lag. Trigger = `group-has-[textarea:focus,.compose-send:focus]` (NOT `group-focus-within`): fires only when the textarea OR the **send** button (marked with the `compose-send` class) is focused — never the Ask-AI pill/bookmark/attribution, and (narrowed 2026-06-26) **never the hands-free mic**. The `compose-send` marker keeps the no-flicker-on-submit behavior. **Why narrowed:** the selector used to be `.relative button:focus` (ANY button in `div.relative`), but the mic + send swap in that same slot, so tapping the hands-free mic wrongly triggered the collapse — the compose area shifted/hid the lower rows as if the keyboard had opened. Keying on the send-only `compose-send` marker fixes it (mic has no marker → can't trigger). **DO NOT** add `compose-send` to the mic or any other compose control. **Constraints:** keep any tappable control OUT of a region whose own focus triggers the collapse; keep the `.relative`-scoped selector valid (no other focusable `.relative button` added inside the compose group). Desktop (fine pointer) unaffected. Pending final iPhone confirm (Ask AI opens first-tap; dock works; no send flicker). Optional future mitigation for (a): collapse the Bootboard to a slim strip on focus (same lag-free CSS) to shrink the visible scroll-off.
- **Mic: record + Groq Whisper, NOT the Web Speech API (settled 2026-06-25, Phase 8).** The voice-to-text mic was rebuilt. The old `webkitSpeechRecognition` (Web Speech API) is UNFIXABLE on iPhone — WebKit deferred it in home-screen PWAs indefinitely (bug #225298, "resolved later"), it's blocked in all non-Safari iOS browsers (WKWebView), absent in Firefox, and depends on iOS system Dictation being on (the `service-not-allowed` we hit). No code change fixes it — it's Apple policy. ChatGPT's web mic is confirmed (OpenAI help docs) to use server-side Whisper, not the browser API. **New architecture (the "ChatGPT way") — works on iPhone Safari + installed PWA + Android + desktop + Firefox:** `src/hooks/useVoiceToText.ts` records audio (`getUserMedia` + `MediaRecorder`) and POSTs it to `src/app/api/transcribe/route.ts`, which forwards to **Groq Whisper Large v3 Turbo** (OpenAI-compatible endpoint) and returns `{ text }`; `PostForm` inserts it via the kept `dispatchEvent('input')` trick (the one piece that makes a programmatic `.value` write visible to React). **iOS must-dos baked in:** `getUserMedia` called synchronously in the tap handler (user-gesture requirement); runtime MIME detection (`MediaRecorder.isTypeSupported` — iOS only supports `audio/mp4`); **`recorder.start(1000)`** (1-second timeslice — a single unchunked Safari mp4 blob otherwise garbles Whisper); drop empty chunks; filename extension matched to the codec. **Cost:** Groq free tier 2,000/day (no card) covers launch, ~$0.04/hr of audio after — negligible. The route mirrors `/api/agent`'s cost guards (per-IP rate limit + concurrency cap + `TRANSCRIBE_DAILY_LIMIT` circuit-breaker); returns 503 if `GROQ_API_KEY` is unset (the mic degrades to a "voice input offline" toast, nothing else breaks). **DO NOT revive the Web Speech API** — it cannot work on iPhone PWAs. **Confirmed working on Android 2026-06-26 AND iPhone (Safari + PWA) 2026-06-26 (owner: "looks right"); desktop pending.** Env: `GROQ_API_KEY` (+ optional `TRANSCRIBE_DAILY_LIMIT`). **Mic UX (2026-06-26):** idle mic is amber-tinted + amber ring (a discoverable voice affordance); recording is red `animate-pulse` (unambiguous "live"); transcribing is amber + spinner — one coherent control. The mic is **HANDS-FREE — it does NOT auto-open the keyboard after dictation** (text appears, send button ready); `handleTranscript` calls `el.focus()` ONLY on a fine pointer (desktop keep-typing), never on touch — so dictation never pops the keyboard or triggers the #6 scroll-off. **Post-send refocus is likewise touch-gated (2026-06-26):** `performSubmit` + the post-completion `isPending` effect refocus the textarea only on a fine pointer, so posting on touch doesn't re-pop the keyboard either (the whole compose area is hands-free on touch; desktop keeps the caret for continuous posting). Tapping the mic while a keyboard is open is ALLOWED to close it (switching to hands-free), do NOT `preventDefault` the focus-steal. **Gotcha that cost ~1h:** a persistent Groq `401 Invalid API Key` turned out to be an **UNSAVED `.env.local`** — the editor buffer showed the new key but the file on disk still held the old one, so the app read the stale key. The temp key-metadata debug log (length/prefix/suffix, never the secret) caught it: the app loaded a 68-char key while the editor showed the correct 56-char one → the on-disk value was stale. A `.trim()` on the key read is kept (whitespace defense). Lesson: if a Next env var seems wrong despite `.env.local`, confirm the file is actually **SAVED to disk** AND restart (`.env.local` is read only at server boot).
- **Phase 8 device-QA UX decisions (settled 2026-06-24).** From owner Android/desktop testing — recorded so they are not reverted:
  - **Feed scroll:** the user's OWN post always scrolls to it and sticks through the ~500ms confirmation (`useScrollTracker.markJustPosted`, fired from `Feed` on optimistic-post add); other users' polled posts NEVER yank the scroll — they go to the unread badge. Replaced the old `isAtBottom`-gated auto-scroll (it flip-flopped once the mobile keyboard shrank the viewport). **DO NOT** re-add isAtBottom auto-scroll for polled posts.
  - **Enter key:** on touch devices (`pointer: coarse`) Return inserts a newline (post via the send button); desktop keeps Enter-to-post / Shift+Enter-newline (`PostForm`).
  - **Modal backdrop:** the content-creating modals (ProtectModal, ChangePassphraseModal, RestoreModal) do NOT dismiss on outside-tap (accidental loss of in-progress entry) — exit via X/Cancel only. SignInModal + FundAddress keep backdrop-dismiss (lower-stakes). Orthogonal to the tab-blur/session-clear teardown — do not touch that.
  - **Mobile keyboard:** passphrase inputs `onFocus` scrollIntoView so they sit above the Android keyboard (ProtectModal, ChangePassphraseModal, shared PassphrasePrompt).
  - **Pending on the chip — compatible with "Balance shows spendable (confirmed)", NOT a reversal:** when the spendable balance is 0 but a payout is landing (0-conf), the chip shows a muted "+X pending" instead of "$0.00" (so a first earner SEES they got paid, before the save toast); once a spendable balance confirms, the chip shows ONLY that. Pending is a SEPARATE muted element, NEVER summed into the spendable headline — which is exactly the "pending shows as a muted line" the balance-spendable decision already sanctions. `pendingSats` was already fetched. First-earning toast still fires on earning (NOT gated on confirmation — BSV ~10min confirm vs ~30s save; gating it would prompt to an empty room). **DO NOT** sum pending into the spendable number. **Refined 2026-06-25 (iPhone QA #3/#4):** the pending readout was moved OFF the inline chip to a muted line BENEATH it (absolute, `pointer-events-none`) — the inline version widened the chip into the centred "Origin" nav button. The first-earning toast now WAITS until pending is actually visible on the chip (fires +600ms after) with an 8s fallback; the balance is refetched the instant earnings land so pending appears within seconds. Still gated on 0-conf pending, NOT block confirmation.
  - **$ formatting:** dollars stay the default; the shared dynamic-decimal `satsToDollars` is the single formatter everywhere (no hard 2-decimal cap — sub-cent earnings must not display as "$0.00"). RestoreModal was the last holdout (inline `.toFixed(2)`), now routed through `satsToDollars`.
  - **ChangePassphrase copy:** changing the passphrase does NOT invalidate the old recovery file (encrypt-in-place, same key) — copy must say the old file still works with the old passphrase + delete old copies if it leaked. **DO NOT** claim the old file "stops working" (false + security-misleading).
  - **Recovery-file preview:** the "can't unlock in this preview" notice sits BELOW the decrypt panel + hides synchronously in JS-on browsers (no flash), visible-by-default in JS-off previews (inverse-noscript — no `<noscript>`). Button is "Decrypt" (not "Decrypt all"). Address stays a tap-to-select `<input readonly>`; no Copy button on any surface (clipboard fails in mobile file previews). Auditor-verified PASS.
- **Phase 8 iPhone-QA UX decisions (settled 2026-06-25).** Keyboard-viewport (#6) and pending-chip (#3/#4) are recorded in their own entries above; the rest:
  - **Memory-clue warning goes red on type:** the hint helper text ("Only you should know what this means — it's stored unprotected…") turns RED once the hint field is non-empty (ProtectModal + ChangePassphraseModal). The hint is stored UNPROTECTED (plaintext) in the recovery file, so the warning escalates the moment the user starts writing one.
  - **Restore wording:** user-facing "Restore" ENTRY points are labelled **"Upload your saved file"** (welcome-gate button, RestoreModal title, You-modal row, SignInModal "can't sign in?" link) — plainer for non-technical users. **KEEP "Restore" on the final post-passphrase ACTION button** (at that point you're restoring, not uploading).
  - **App-icon — full-bleed amber + black medallion (2026-06-26, supersedes the earlier band attempts):** the home-screen/install icon (`public/icon.svg` → `icon-192` = apple-touch-icon, `icon-512`, `src/app/favicon.ico`) is **full-bleed amber-400 `#fbbf24`** (matches the "Open" wordmark — the old `#f59e0b`/amber-500 read too orange) with a **black center medallion** (`circle r=196`) holding a large centered "OC" (O amber / C white, `font-size 228`, `dominant-baseline central`). **Why full-bleed:** the earlier inset edge (first a thin 3px stroke, then an 18px band) kept getting **clipped to a hairline by Android's adaptive-icon crop** ("looked like an error line"). With amber bleeding to EVERY edge, any mask (Android circle/squircle, iOS squircle, desktop rounded-square) just crops solid amber — there is no thin edge left to clip. The medallion sits inside Android's ~80% safe-zone. `maskable` manifest entry declared (`purpose: "maskable"` on `icon-512`). Regenerate ALL assets via `node scripts/generate-icons.mjs` — it emits the 192/512 PNGs **and** `src/app/favicon.ico`. The recovery file embeds a SEPARATE copy (`backup-template.ts` `ICON_SVG`) — **keep it in sync** (it also uses amber-400 now). After regen the OS caches the install-time icon, so **uninstall/reinstall the PWA** to see it. **Do NOT go back to an inset band** — it clips on Android; full-bleed is the fix.
  - **Merge the two identity-card actions (#1) — DECIDED NOT TO.** Owner reviewed the agent's "one adaptive CTA" design (merge "not protected" + "save recovery file" into a single `secured = protected && backedUp` gate) and chose to LEAVE the two surfaces as-is. Do not merge without re-raising with the owner.
- **revalidatePath removed:** ISR `revalidate = 10` handles cold loads for new visitors. Polling handles active users. revalidatePath was adding 50-200ms of blocking server work per action with zero user benefit
- **BSV SDK caching:** Client-side SDK loaded once via singleton promise (`getBsvSdk()`), kicked off on page load. PrivateKey parsed from WIF once per session. Eliminated ~280ms cold import + repeated BigNumber work on every post
- **Pagination:** Cursor-based by post ID (not timestamp — IDs are monotonic, no collision risk)
- **Deployment:** Railway with persistent /data volume for SQLite. Dockerfile as alternative. DB path via DATABASE_PATH env var
- **PWA:** manifest.json + SVG icon. No service worker / offline support yet — just home screen install

## Critical Bugs Known

| # | Issue | Severity | Status |
|---|-------|----------|--------|
| 1 | Server never verifies signatures | Critical | FIXED (2026-03-25) — ECDSA verification via @bsv/sdk |
| 2 | No rate limiting | Critical | FIXED (2026-03-25) — in-memory sliding window per-author |
| 3 | WIF displayed raw in DOM | High | FIXED (2026-03-25) — masked by default, reveal toggle |
| 4 | Backup file contains raw WIF | High | TODO |
| 5 | JSON.parse without try/catch | Medium | FIXED (2026-03-25) — returns null on parse failure |
| 6 | Database file in project root | Low | TODO |

## UTXO Management (settled)

- **Fee model: 100 sat/kB everywhere (settled 2026-04-13; rate re-confirmed live 2026-06-19).** Always pass `new SatoshisPerKilobyte(100)` explicitly to `tx.fee()` across ALL tx paths — boots, consolidation, sweeps, transfers. No split fee tiers. **100 sat/kB is the current live ARC miner floor** (GorillaPool `/v1/policy` `miningFee` = 100 sat / 1000 bytes, verified 2026-06-19) — the minimum that reliably clears, NOT an overpay, so there is no safe room below it. The old split tier let *consolidation/sweeps only* run at 10 sat/kb to save ~120 sats per tx; that contributed to slow confirmations which cascaded into ORPHAN_MEMPOOL, mempool-conflict, and multi-day unconfirmed chains. **Correction (2026-06-19): the server post-logging and boot-payment paths were NEVER on the 10 sat/kb tier — they always used 100 sat/kB — so that cascade rationale never applied to them; this entry only unified the consolidation/sweep tier *up* to 100.** Verified per-post cost at 100 sat/kB (byte-exact from the @bsv/sdk fee model): ~35 sats unsigned-short / ~66 sats typical-signed / ~156 sats max-1000-char — at any realistic volume a rounding error vs the ~$50/mo budget (~217k posts/day funded at $11.62/BSV, 2026-06-19). Not worth the complexity or risk of going lower.
- **Consolidation strategy (Option F — "sweep on boot"):** `selectUtxos()` uses smallest-first ordering and includes up to 20 inputs per boot transaction. Users with many tiny UTXOs consolidate ~20 per boot for free via the change output. 290 UTXOs → fully consolidated in ~15 boots. Cost per consolidation: marginal extra tx bytes, no separate consolidation transaction needed.
- **No server custody:** Options A/C/D (server consolidation, held payouts, threshold batching) were all rejected — they require server custody or break the trustless model.
- **Dust threshold:** During UTXO selection, once boot + fee is covered, additional inputs are only included if their value exceeds the marginal fee cost of adding one more input (~15 sats at 100 sat/kb). Absolute dust is never swept.
- **MAX_CONSOLIDATION_INPUTS = 20:** 20 P2PKH inputs ≈ 2,960 bytes ≈ 296 sats at 100 sat/kb. Stays well under the boot price floor (1,000 sats minimum), leaving room for the payout in the same transaction. Tunable constant in `client-boot.ts`.
- **DUST_THRESHOLD = 16 sats (updated 2026-04-13).** Minimum UTXO value worth sweeping during consolidation. At 100 sat/kb, each P2PKH input costs ~15 sats. Was 2 sats when consolidation ran at 10 sat/kb; was 10 before that (which trapped 3–9 sat UTXOs). MAX_CONSOLIDATION_SWEEP = 200 caps pathological inputs. **Linked to fee rate:** if the fee model changes (miner partnership, rate reduction, or increase), recalculate DUST_THRESHOLD as `ceil(148 × rate_per_byte) + 1`. At 50 sat/kb → 8 sats, at 10 sat/kb → 2 sats, at 1 sat/kb → 1 sat. Also update the boot-time dust threshold in UTXO selection (~15 sats at current 100 sat/kb). Both thresholds are in `client-boot.ts`.
- **Optimistic UTXO blacklisting — REMOVED (2026-04-13).** Originally added 2026-04-10 to prevent state poisoning from failed broadcasts. Removed because it caused permanent wallet lockout: failed broadcasts left inputs blacklisted in localStorage with no automatic recovery. Double-spend prevention is now handled by: (1) mutex serializing boot calls, (2) 0-conf chaining via `_pendingChange` (own change outputs with sourceTransaction attached), (3) 3s UI boot throttle. Inputs are now blacklisted ONLY on successful broadcast.
- **3-second UI boot throttle (settled 2026-04-11).** Boot button is disabled for 3 seconds after each click. Eliminates the entire class of "user clicks faster than the network propagates" edge cases (orphan races, mempool conflicts, double-spend attempts) at zero code complexity. Architecture review recommendation — chosen over a 50-line DOUBLE_SPEND_ATTEMPTED handler that would have solved the same problem with much more complexity.
- **Reset Wallet via key rotation (settled 2026-04-11, updated 2026-04-13).** When a user wants to rotate to a fresh address, `sweepFunds` (formerly `sweepConfirmedFunds`) transfers ALL UTXOs (confirmed + unconfirmed) from the old address to the new one. At 100 sat/kb all txs confirm in next block, so unconfirmed UTXOs are safe to include. Migration is registered on-chain via existing migration.ts pipeline. Implemented as `MoveAddressModal` — a 4-stage wizard that auto-advances with visible status at each step. Label is "Move to a new address" (not "Reset Wallet") per UX review.
- **Deferred localStorage commit for key rotation (settled 2026-04-12).** `resetIdentity()` accepts `{ deferCommit: true }`. The new key is NOT written to localStorage until the caller (MoveAddressModal) confirms all stages succeeded — sweep broadcast + migration recording. Without this, a failed sweep would strand funds: localStorage updates to the new (empty) key while the old key's funds are unreachable. This bug caused 45,558 sats to be stranded during testing — recovered only because the auto-download backup of the old key (Stage 1) preserved access to the old address. This validates the design of downloading the old key BEFORE rotation as a critical safety net, not just a convenience.
- **Broadcaster: ARC everywhere (settled 2026-04-13).** All broadcast paths use ARC (@bsv/sdk default `tx.broadcast()`) — `clientSideBoot`, `consolidateUtxos`, `sweepFunds`, `autoTransferFunds`, and server wallet's `buildAndBroadcast`. Previously a mix of ARC and WhatsOnChainBroadcaster — the WoC switch was based on a misdiagnosed ARC outage (actually a local DNS cache issue, resolved by rebooting the PC). ARC sends txs directly to GorillaPool (the miner), provides structured error responses (ORPHAN status, competingTxs for double-spend), and supports 0-conf chaining via BEEF. WoC is used only for read operations (UTXO fetching, source tx hex, balance, exchange rate) — not broadcasting.
- **Server-side source tx cache (settled 2026-04-13).** `/api/tx-hex/route.ts` maintains an in-memory Map (~2000 entries, LRU eviction) of fetched source tx hex. Source tx hex is immutable, so cache-forever is correct. Eliminates repeated WoC calls for the same txid across boots, sweeps, and consolidations. Without this, a boot with 15+ inputs fires 15+ parallel WoC calls, exceeding WoC's ~3 req/s limit and causing 429 errors.
- **Explicit backup acknowledgement (settled 2026-04-16).** `backedUp` no longer flips on download trigger alone — the user must click a "Got it" confirmation after the file is offered. Applies to both paths: the You dropdown (green confirmation banner replaces the orange save-CTA) and `MoveAddressModal` (new `saved-confirm` stage gates the auto-advance to the irreversible sweep). Rationale: silent download failures (popup blocker, disk full, CSP deny, user cancels save dialog) previously masqueraded as success and cleared the warning dot — leaving users with no backup and no visible prompt. Commit `e7ecf9f`.
- **Unconfirmed-parent 404 retry in `/api/tx-hex` (settled 2026-04-16).** When WoC returns 404 on a `/tx/{txid}/hex` lookup, the parent may be a just-broadcast 0-conf chain ancestor that WoC hasn't indexed yet (typical lag: 2–10s). The proxy now retries 404s up to 3 times with 2s backoff (~6s total budget) before giving up. Kept as a separate counter from the 429/5xx retry budget so a genuinely missing tx still fails fast after the 6s window. Motivated by `fetchSourceTxsBatched` sweeps that 404'd on their own just-broadcast change parents during `MoveAddressModal` / `clientSideBoot`. Future optimization (deferred): cache own broadcast `rawTx` client-side and pass through so sweep paths never need to round-trip through WoC for their own parents.
- **Batched source tx fetches in clientSideBoot (settled 2026-04-13).** `clientSideBoot` fetches source transactions in batches of 5 with 1s inter-batch delay (matching `consolidateUtxos` pattern). Previously used bare `Promise.all` which fired all fetches simultaneously, triggering WoC rate limits on users with many UTXOs.
- **Deferred session cache in upgradeIdentity (settled 2026-04-12).** `upgradeIdentity()` no longer sets `_sessionIdentity`/`_cachedWif`/`_cachedPrivateKey` eagerly. `commitUpgrade(encStore, identity)` now accepts an optional identity and commits the session caches atomically with the localStorage write — only after `migrateIdentity()` server action succeeds. Matches the deferred commit pattern in `resetIdentity()`. Prevents in-memory signing key from diverging from server's identity record on migration failure.
- **Balance poll interval: 30s (updated 2026-04-13).** `IdentityBar.fetchLiveBalance` polls every 30 seconds (was 15s). Halves the background WoC request rate to reduce 429 pressure from normal page sitting. As of 2026-04-14 it polls `/api/balance` (server-proxied), not WoC directly.
- **Server-side WoC read proxies (settled 2026-04-14).** `/api/balance` (10s TTL, 120 req/min per IP) and `/api/unspent` (3s TTL, 180 req/min per IP) join `/api/tx-hex` as cached server proxies for all WhatsOnChain read traffic. Direct browser→WoC reads are gone. Rationale: WoC rate limits at ~3 req/s per IP; a paid boot with 15 UTXOs + background balance polls + other tabs easily exceeded this, producing 429 cascades that broke boots and froze balances. Server-side caching collapses N client requests to 1 upstream request within the TTL window and isolates the browser from WoC's rate policy. Both proxies retry 429/5xx with stale-cache fallback. IdentityBar falls back to last-known balance (not 0) on transient errors so the UI doesn't flash empty.
- **Boot confirmation via local tx parsing (settled 2026-04-14).** `/api/boot-confirm` no longer fetches the tx from WoC to verify outputs. Client sends `rawTx` alongside `txid`; server verifies `hash(rawTx) === txid` (self-authenticating) and parses P2PKH outputs locally from the raw bytes to check the split matches. Server also re-broadcasts via ARC as a safety net. Eliminates the 5–30s WoC indexing lag that previously produced false TX_NOT_FOUND errors on freshly broadcast boots, and removes another WoC rate-limit chokepoint from the critical boot path. Returns explicit `TX_CONFLICT` vs `ARC_UNAVAILABLE` error codes so the client can distinguish fatal conflicts from retriable upstream issues.
- **Structured error codes in broadcast detection (settled 2026-04-14).** Broadcast error classification matches against a structured `code` field on ARC responses, not substring search on the message. Prior substring matching against e.g. "257" produced false positives inside unrelated txids/timestamps, mislabelling successful broadcasts as conflicts. See `client-boot.ts`.
- **Server wallet shares the client's resilience stack (settled 2026-04-15, build deferred).** Server-side tx paths (`wallet.ts`, `onchain.ts`, `boot-orchestrator.ts`) must go through the same `/api/broadcast` proxy and shared WoC read cache as the browser — not direct `tx.broadcast()` or raw `fetch` to WoC. Rationale: the mutex in `wallet.ts` serializes ALL server-side on-chain activity (posts + free boots) through one broadcast call. An ARC hang without timeout freezes the entire platform for the hang duration. **The timeout must cover every in-mutex network call, not only the broadcast** — `wallet.ts` makes four un-timed external calls inside the held mutex (unspent `:89`, source-tx-hex `:167`, broadcast `:267`, double-spend recovery `:323`); any one hanging freezes posts + free boots site-wide. The same ~10s `AbortController` discipline applies to all four; on timeout, release the lock and let existing retry logic re-run. A dropped 0-conf chain fan-outs into uncached WoC reads that can trigger 429s and stall recovery. The browser was hardened against both during the 2026-04-13/14 work; the server path was not. Rule: if a resilience mitigation exists for a client path, it must also cover the server path that does the equivalent external call. See ROADMAP Phase 6.5 for build-spec (proxy, shared cache module, broadcast timeout, queue-depth metric, low-balance alert).
- **Real-time updates: SSE is enhancement, polling is ground truth (settled 2026-04-15, build deferred).** When implemented, the planned `/api/events` SSE channel and optimistic own-action updates are a UX accelerator on top of the existing 30s polling, NOT a replacement for it. Polling (`/api/balance`, `/api/earnings`, `/api/earnings?summary=1`) stays as the authoritative source. SSE events trigger an early refetch; they do not directly mutate canonical state. Rationale: SSE can drop silently (connection, edge cold-start, in-process emitter on a non-subscribed instance); optimistic balance can diverge from chain reality if a tx is orphaned or replaced. Polling self-heals both within one tick. Corollary: **do not remove the polling loops when SSE lands** — they are the fallback that makes SSE safe to fail. Also: emit-site must be wrapped in a `publishPayout()` helper so the in-process EventEmitter can be swapped for Redis pub/sub or Postgres LISTEN/NOTIFY when the app goes horizontally scaled, without touching any caller. See ROADMAP Phase 6.5 for the build-spec. **Horizontal-scale checklist (more than the emitter):** the server wallet's mutex + UTXO reservation (`_reserved`/`_pendingChange`) + double-spend blacklist (`_spent`) are also in-process memory (`wallet.ts:14,65-67`). Running 2+ instances against one `BSV_SERVER_WIF` lets instances spend the same UTXOs (double-spend errors, failed boots/posts) — a correctness break, not just a dropped UX event. Single instance is safe; multi-instance is a hard prerequisite, not a launch blocker. Cheap first step is a single global advisory lock around `buildAndBroadcast`; the full fix relocates reservation + blacklist state to shared storage. Same rule as the emitter: the swap must be possible without touching callers.
- **UTXO outpoint dedup on sweep paths (settled 2026-06-03).** `autoTransferFunds` and `sweepFunds` in `identity.ts` MUST run the raw `/api/unspent` response through `dedupeUtxos()` before building the tx. WhatsOnChain has been observed returning the same `(tx_hash, tx_pos)` outpoint twice; building a tx from that list produces `bad-txns-inputs-duplicate` peer rejection (structural malformation, not double-spend). Same pattern `client-boot.ts` already enforces via its `utxoKey` Set. Two distinct UTXOs cannot share an outpoint (Bitcoin protocol invariant), so dedup only removes structural duplicates and is safe to call unconditionally. Do NOT remove the dedup helper as "redundant" — the upstream indexer is not a contract we control, and the cost is one Map allocation per sweep. Confirmed Android-side bug 2026-06-03 with same txid `8fc71ef6…` rejected twice in a row, third attempt succeeded after WoC indexer stabilized.
- **Mobile modal viewports use `svh`, not `vh` (settled 2026-06-03).** Modal containers must use `svh` (small viewport height) instead of `vh` for `max-h` and top-offset values. Reason: Android Chrome's `100vh` includes the collapsible address bar, so `80vh` can exceed the visible area when the toolbar is showing — the user sees the modal's bottom half while the top is scrolled out of view. `svh` excludes browser chrome, ensuring the modal always fits the visible viewport. Canonical pattern: `pt-[6svh]` outer + `max-h-[80svh]` inner card. Applied site-wide 2026-06-03 across 7 centered modals: `MoveAddressModal`, `ChangePassphraseModal`, `StaleKeyModal`, `SignInModal`, `RestoreModal`, `FundAddress`, plus the `IdentityBar` You modal. The `IdentityBar` dropdown (line 1127) is `absolute`-positioned anchored under the chip — different shape, intentionally still on `max-h-[85vh]`. Static HTML recovery files (`backup-template.ts`) use **NO viewport units at all** — **CORRECTED TWICE (2026-06-26):** the original "keep 100vh, no mobile chrome problem when opened locally" assumption was wrong (Android scroll-jitter / jump-to-top as the URL bar collapses); the first fix (`100vh`→`svh` fallback) *also* didn't reliably hold — the owner's file viewer can apply the `100vh` fallback, and `svh` support in Android WebView/preview surfaces is unreliable. **Final fix:** dropped the body `min-height` entirely and moved the dark background to `html` (so a short page still shows no white area) → there is NO `vh`/`svh`/`dvh` anywhere for the collapsing Android URL bar to reflow against. The min-height was only ever doing background-coverage on short content (no vertical centering — the flex column has no `justify-content`), so removing it is layout-safe. **Do NOT reintroduce a viewport-unit min-height in the recovery file.** **AND the actual residual cause (2026-06-26):** even with viewport units gone, a FRESH file still jittered on Android. The real culprit was **CSS scroll-anchoring** — the `#quicklook-notice` renders visible then JS hides ~150px of it, and Android Chrome's scroll-anchoring repeatedly shifts the scroll to compensate (re-fired as the URL bar animates). Fix: **`overflow-anchor: none`** on the recovery `body`. iOS Safari doesn't scroll-anchor this way → iPhone was always fine. **Do NOT remove `overflow-anchor: none` from the recovery body.** **AND the reflow source itself, eliminated (2026-06-26):** `overflow-anchor: none` alone still wasn't enough — so we also hide the amber `#quicklook-notice` **before first paint** via a head-level `<script>document.documentElement.classList.add('js')</script>` + CSS `html.js #quicklook-notice { display:none }`. The notice now never shows-then-collapses in JS browsers (no ~150px reflow at all), while iOS Quick Look (no JS) still shows it (no `.js` class). This is the inverse-noscript pattern moved to head-level. **Do NOT remove the head `.js` script or the `html.js #quicklook-notice` rule.** **AND the ACTUAL root cause (4th pass, 2026-06-26):** none of the above fixed it because the real mechanism is different — **the recovery page is only ~one phone-viewport tall**, so on Android Chrome the only scrollable distance is the URL-bar-collapse zone, and dragging it triggers **overscroll + dynamic-viewport re-clamping that snaps the page back to the top, repeatedly** (the jitter). Two fixes: (1) **`overscroll-behavior-y: none`** on `html, body` (kills the bounce/snap on the root scroller — the documented Android fix for a barely-scrollable page); (2) **`overflow-anchor: none` must be on `html`, not just `body`** — the earlier body-only placement was a **no-op** because `html` is the document scroller. iOS rubber-bands instead of re-clamping → iPhone was always fine; both are no-ops on iOS. **Do NOT remove `overscroll-behavior-y: none` or move `overflow-anchor: none` off `html`.** Secondary (the owner's "decrypt jumps above the keypad" lead): the passphrase input's `onfocus` `scrollIntoView({block:'center'})` yanks on focus — a distinct keyboard-open jump, left in place for now. **CONFIRMED WON'T-FIX (2026-06-26):** with the latest code verified IN the file (a temporary `b6` marker on the offline badge — since removed), the jitter STILL persists on Android. So it is the **inherent Android Chrome URL-bar / overscroll behavior** (web-confirmed: a known Chrome issue, fixable only via user-side Chrome flags — [issuetracker 256787951]), NOT anything the static recovery file can control. We exhausted every CSS lever: viewport units → scroll-anchoring (`overflow-anchor` on html) → notice-reflow (pre-paint hide) → `overscroll-behavior-y`. iPhone is fine (iOS rubber-bands instead of re-clamping). ACCEPTED — it's a rarely-opened offline backup file. **Do NOT reopen this without genuinely new evidence.** (All the CSS hardening above stays — it's correct and harmless — we just stop chasing the residual jitter.) Browser support: Chrome ≥108, Safari ≥15.4, Firefox ≥101 — all covered. **Do NOT add new centered modals on `vh`** — anchor on this pattern.

- **Every key rotation produces an encrypted key (settled 2026-04-17).** `resetIdentity()` (plaintext key rotation) removed from primary UI. Both "Not protected" and "Move to new address" now route through `upgradeIdentity()` via MoveAddressModal, which collects a passphrase first. Users are always protected after rotation. Plaintext `resetIdentity` remains in the codebase as an emergency recovery escape hatch only. Rationale: each rotation risks chain breaks — fewer rotations (one instead of two for move-then-protect) = fewer opportunities for orphaned posts. The combined flow also downloads an encrypted backup automatically, reducing the number of recovery files a user must manage.
- **Identity sweep functions use the /api/unspent proxy (settled 2026-04-17).** `sweepFunds()` and `autoTransferFunds()` in `identity.ts` no longer hit WoC directly. They use `/api/unspent?fresh=1` which provides retry, stale-cache fallback, and rate-limit protection. Rationale: a single WoC 429 or empty response during a sweep previously caused silent fund loss — the function returned "no funds, not an error" and the rotation committed, stranding sats on the old address with the key deleted from localStorage.
- **Pre-rotation chain verification (settled 2026-04-17).** `verifyMigrationChain(pubkey)` server action checks that all posting pubkeys resolve to the current key via the migrations table before any key rotation. If orphaned pubkeys are detected, the user is warned with a "proceed anyway" escape hatch. Rationale: migration chain breaks silently orphan posts from the fairness system, dropping earnings from ~590 to ~11 sats per split with no visible error.
- **Memory clue is mandatory (settled 2026-04-17).** The passphrase hint field is required in MoveAddressModal (passphrase stage) and ChangePassphraseModal. Submit button disabled until filled. No minimum length — a user who types "x" has made a conscious choice, but skipping entirely is blocked. Rationale: OpenCook has no email/phone recovery channel; the hint is the only lifeline if a user forgets their passphrase.
- **Single-passphrase manage gate (settled 2026-04-30).** The You modal verifies the passphrase once on entry (`manageAuthed` state); session destroyed on modal close OR tab blur (password-manager pattern). Move + Passphrase actions unlocked while open without re-prompting. Rationale: passphrase fatigue across the merged Passphrase / Move flows — both call the same primitives, both require re-auth at the action level was redundant. Tab-blur destroy is the lightweight session-timeout primitive (full app-wide auto-lock with timer remains in FUTURE.md). **Do not re-add per-action prompts to Passphrase/Move** without superseding this decision.
- **Combined recovery file (settled 2026-04-30).** Stage-3 download in MoveAddressModal contains both `wif_encrypted` (new key) AND `oldWif_encrypted` (old key under new passphrase). One file, one passphrase, both keys recoverable. Supersedes the temporary stage-1 file (which the done-state copy explicitly tells the user to delete). Rationale: the prior pattern produced two files per rotation, increasing the chance the user kept the wrong one or none. The combined file also keeps the old key available for any unconfirmed UTXOs that miss the sweep, without leaving plaintext exposure.
- **Asymmetric acknowledgement on Show recovery key + Restore is intentional (settled 2026-04-30, refined 2026-05-01).** Despite the manage-gate session being valid, "Show recovery key" requires an explicit Reveal-acknowledge click (with a red warning above the masked key) before the WIF is shown, and "Restore" still re-prompts for the encrypted file's passphrase. Both are **defense-in-depth on highest-stakes paths** — irreversible reveal of the raw WIF / overwrite of the local identity. Stage 8 reworked Show recovery key from a passphrase re-prompt to an in-panel `[Reveal key]` acknowledgement gate; the spirit of the asymmetry holds (the user must do something deliberate to expose the key), even though the mechanism shifted. **Do not "fix" this asymmetry for consistency in a future refactor** — cheaper actions (Passphrase, Move) inherit the gate, but reveal/overwrite require their own deliberate action.
- **Wizard auto-close split (settled 2026-04-30, refined 2026-05-01 + 2026-05-04).** `MoveAddressModal.onComplete` updates identity state only and sets `moveCompletedRef.current = true` (parent does not unmount the wizard); `onClose` (Continue button / X / backdrop on done, OR Cancel mid-wizard) is the single dismissal path that always closes the wizard. **On successful completion (`moveCompletedRef.current === true`), `onClose` calls `closeManageModal()` to close the You modal entirely** — the user has just seen the wizard's done state and downloaded the recovery file, so dropping them back into a freshly-locked You modal adds nothing useful. They land on the page with the updated chip. **On Cancel mid-wizard, the You modal stays open** under the original passphrase (manage gate intact). RestoreModal applies the same parity — `onSuccess` also calls `closeManageModal()` because the modal would otherwise show the previous identity's stale state. Rationale: prior `onComplete = close everything` pattern dismissed the wizard the instant `setStage("done")` fired, so the user never saw completed steps + sats-moved + safeguard copy — that load-bearing rationale lives INSIDE the wizard and is preserved. The "keep You modal open underneath" half was incidental; closing it post-success eliminates the entire "old vs new passphrase cache" question (originally addressed by re-locking and clearing `reAuthPassphraseRef`, but re-locking forced the user to re-type a passphrase they typed three times in the last 30 seconds — friction with no information payoff). Ref (not state) for `moveCompletedRef` to avoid stale-closure if React batches setStates before onClose runs. **Considered and rejected:** propagate the new passphrase from the wizard up via an extended `onComplete(identity, newPassphrase)` signature so the manage gate can stay unlocked under the new passphrase — works, but solves at the wrong altitude (touches the wizard's prop signature, threads passphrase through state, leaves the user in a You modal they have no remaining business in). Closing entirely is the simpler fix.

- **Locked-state You modal pattern (settled 2026-05-01).** The manage gate is no longer a separate stacked modal. The You modal opens directly for protected users in `mode === "locked"` with a passphrase prompt as the body. On unlock, the body cross-fades to the rows (Save / Passphrase / Restore / Show recovery key). One container, two states. Same modal frame, same backdrop, same gold stripe — only the body content changes. Cross-fade via `animate-[fadeIn_0.2s_ease-out]` with distinct keys per state. Rationale: two stacked modals for one password field was disorienting; iOS Settings + Face ID precedent says "destination appears immediately, locked, unlocks in place." **Do not re-introduce a separate gate modal** — the locked-state pattern is the durable architecture. Implementation in `IdentityBar.tsx`: `mode` state, `manageAuthed` derived as `mode !== "locked"`, `gateInputRef` for autofocus on locked-state mount, body conditional rendering keyed on `manageAuthed`.

- **Sign-in trigger: centered modal, no global catcher, no auto-replay (settled 2026-05-03, supersedes the ambient-pill + LockedClickCatcher + universal-shake-contract decisions below).** The locked state is invisible: the IdentityChip renders the cached anon name (from `getStoredAnonName()` reading the encrypted store's plaintext `name` field) so the site looks signed in. Read-only actions (AI chat, scrolling, reading posts, typing in textarea, opening menus) NEVER trigger sign-in — that was the decisive reason for deleting `LockedClickCatcher`, which fired on every interactive pointerdown. The ONLY trigger is a transaction-attempting action: when a user taps boot, hits Enter on a post, or in future taps tip / play / fund, the action's handler calls `requireIdentity()`, which opens a centered `<SignInModal>` if locked and returns `false`. User signs in → modal closes → user retaps action. **No auto-replay.** Per-handler line: `if (!requireIdentity() || !identity) return;` — one line per transaction handler, in the handler's own file. **Architecture:** `requireIdentity()` lives on `IdentityContext` alongside `signInOpen`, `openSignIn`, `closeSignIn`. `<SignInModal>` mounted in `Feed.tsx` reads `signInOpen` and renders. On success the modal calls `unlockIdentity()` + `updateIdentity()` then `closeSignIn()`. Wrong-passphrase shake is LOCAL state in the modal, not context. Tab blur closes the modal and clears state (password-manager parity). **Considered and rejected:** (a) `requestIdentity(): Promise<Identity>` — dead code without auto-replay, since a promise nobody awaits is a function call; (b) `Wallet` capability abstraction wrapping `clientSideBoot`/`signPost` — wrong altitude, would refactor the most security-sensitive code in the repo (mutex, spent-set, 0-conf chaining) for zero user-facing benefit, and a thin façade over the existing services is a one-afternoon migration if OpenCook ever scales beyond 5 transaction surfaces; (c) marker attribute on transaction buttons — requires rewiring every button's disabled state and keeps a permanent global listener tax. **Do not re-introduce a global pointerdown catcher** — its read-vs-write blindness violates the "reading should be silent" principle that drove this design.

- **[SUPERSEDED 2026-05-03] Locked state: ambient pill + idea preservation (settled 2026-05-03, replaced same day).** Was: ambient `🔒 Sign in` pill in header, click expands popover, shake on locked-action. Replaced by centered `<SignInModal>` triggered only by transaction actions (see decision above). Kept here for the verbiage record: **"Sign in"** (button) and **"Enter"** (action) verbs are still right — neither "Login" nor "Unlock". The "Need a hint?" two-step reveal pattern survives in the modal.

- **[SUPERSEDED 2026-05-03] Universal "transaction action requires sign-in" pattern via `signalLockedAttempt()` (settled 2026-05-03 morning, replaced same day).** Was: every transaction handler started with `if (!identity) { signalLockedAttempt(); return; }`. Replaced by `requireIdentity()` which opens a centered modal instead of shaking the (now-deleted) ambient pill. The "no auto-replay" + "one line per handler" principles carry forward unchanged.

- **Recovery file: static render for iOS Quick Look compatibility (settled 2026-05-04).** User reported that on iPhone, downloading the recovery HTML and opening it from the Files app showed empty fields (no name, address, saved date, or WIF). Root cause: iOS Quick Look (the WebKit-based previewer used by Files, Mail preview, Messages preview, AirDrop preview, AND macOS Finder Quick Look) does NOT execute inline JavaScript in local HTML files for security reasons. The previous template populated every dynamic field via `document.getElementById(...).textContent = BACKUP_DATA.X`, which left the file looking blank in any non-JS viewer. **Fix:** every static-renderable field is now interpolated into HTML at template-build time — name, address, saved date, plaintext WIF, hint, and footer stamp. Only the encrypted decrypt flow remains JS-dependent (it must run `crypto.subtle.decrypt`), and encrypted files now show a `<noscript>` banner instructing users to open in Safari/Chrome/Firefox if they're in a Quick Look preview. **Specifics:** (a) `formatSavedDate` helper uses fixed `en-US` locale (not `undefined`) so output is stable across server locales (Vercel, Railway, Windows dev). (b) `escapeHtml` applied to all user-controlled fields (`name`, `hint`); architect confirmed sufficient for body context — threat model is self-XSS only, not meaningful. (c) `.meta-value` gets `user-select: all` so iOS users can long-press-copy the address even when the JS Copy button is inert. (d) Dead JS removed (don't leave `meta-name`/`meta-address`/`meta-date`/`footer-stamp`/`wif-display`/`hint-text` setters that look load-bearing to future readers). (e) BACKUP_DATA JSON kept as-is in the script tag — slimming it adds risk without benefit; only the encrypted `wif_encrypted` / `oldWif_encrypted` / `oldAddress` fields are read by JS now, but harmless to leave the rest. (f) `<noscript>` banner uses amber `.noscript-banner` style (informational, not red-warning), placed above the decrypt card so users see what they're missing without the card being replaced. **Threat model unchanged.** The WIF was always in the rendered DOM after JS ran AND in the JSON inside `<script>`. Moving it to HTML body adds one more place inside the same file where it lives — but anyone with the file already has the WIF regardless. The plaintext red banner (*"This file is not encrypted. Anyone who can open it can take your account."*) renders statically in both old and new templates, so iOS Quick Look users see the warning before the WIF. Architect explicitly validated this with no security regressions. **Anti-patterns to avoid:** don't move the encrypted ciphertext into HTML body (it'd be silly — JS needs it from JSON for decryption), don't drop the `<noscript>` banner (encrypted files need clear guidance for non-JS viewers), don't switch to `toLocaleDateString(undefined, ...)` at build time (server locale is unstable).

- **Recovery file copy & layout polish (settled 2026-05-04).** Round-2 polish on top of the 2026-05-03 backup overhaul, after user review of the actual file output. Changes: (a) **Public address now appears once**, in the metadata card at the top — duplicate "Current public address" rows removed from inside the WIF blocks, and the metadata Address row gets a small Copy button. The Previous-public-address row stays inside the previous-WIF block because that's the only place it appears. (b) **Per-variant context block** added beneath the metadata card, one or two sentences each, telling the user what THIS file is and where their posts/earnings actually live. Five variants: `save` encrypted, `save` plaintext (overrides + reinforces the red banner), `rotation`, `pre-rotation` (temporary checkpoint), `restore-pre` (snapshot before restore). (c) **Previous-key warning consolidated** from two stacked paragraphs into one that reads: *"⚠ **Previous secret key.** Your posts and earnings have moved to your current address — this key is only here in case any funds were in transit during the move. Treat it with the same care as your current key: anyone who has it controls that address. Never share it — not with support, not with friends, not with anyone."* (d) **"Secret key" terminology** applied where the noun is the key itself (matches the existing `IdentityBar` pattern *"Secret key — handle with care"* on the Show recovery key row). The feature/file remains "recovery" (recovery file, recovery key); the value inside is the "secret key". WIF labels: *"Your secret key (WIF)"* / *"Previous secret key"*. Decrypt label: *"Enter your passphrase to unlock your secret key"*. Current-key warning: *"Anyone who has this secret key controls your account..."*. (e) **Subtitle generic-ised** to *"Keep this file somewhere only you can find it."* — the per-variant context block now carries the variant-specific framing. (f) **"Decryption successful" → "Key unlocked"** for warmer/shorter post-decrypt success header. (g) **Green "Private & Offline" banner removed** as cargo — the offline badge already covers the functional claim, the HTML comment `<!-- No network calls. Verify: View Source. -->` is the actual proof, and three places saying the same thing was bloat. (h) **Footer trimmed** to `bsvibes.com` link plus a small monospace stamp `Recovery file · <pathType> · saved <date>` for support tickets. (i) **Universal `copyText(id, btn)` helper** hoisted out of variant-conditional JS so both the metadata Address row and any in-body Copy button (e.g., previous-address row) share one implementation. Don't redundantly show the public address inside the WIF block — the metadata card is the single source. Don't reintroduce per-key "Anyone who has this key controls..." duplication on rotation files — the consolidated previous-key paragraph carries both the meaning AND the severity.

- **Backup file audit & overhaul (settled 2026-05-03).** All recovery-file downloads route through a single `downloadBackup(data)` API in `services/bsv/backup-template.ts`. Caller passes a typed `BackupData` with required `pathType: "save" | "rotation" | "pre-rotation" | "restore-pre"` and optional `oldAddress`; the filename is auto-built — callers do not pass filenames. **Filename pattern:** `bsvibes-<pathType>-<anon_name>-<addr6>[-to-<newAddr6>]-<YYYY-MM-DD-HHmm>.html`, where `addr6 = address.slice(1, 7)` (skip leading P2PKH `1`, take next 6 chars). `-to-` is used between addresses (not `>`) because Windows reserves `>` in filenames. The anon_name is preserved verbatim (sanitised to `[a-zA-Z0-9_]` with `-` fallback) so users can correlate files to identities. **Combined recovery file is rotation-only (refines the 2026-04-30 decision).** On-demand "Save" downloads from the You modal contain a single key — single-key by design. Rotation paths (MoveAddressModal stage-3 + ChangePassphraseModal completion) produce combined files with `wif_encrypted` (new) and `oldWif_encrypted` (old, encrypted under the new passphrase). ChangePassphraseModal sets `oldAddress: undefined` because the address is unchanged — the filename gets a single `addr6` segment, but the file body still contains both keys. **HTML template changes:** every WIF block now displays its public address above (with a Copy button on the address only — Copy buttons removed from all WIF surfaces, including the post-decrypt result section), a red warning beneath each WIF (`"Anyone who has this key controls your account..."`), an additional "May still hold funds if the transfer was skipped" line above the previous-key warning, and a red `plaintext-banner` above the only path that produces a plaintext WIF (the You-modal "Save" with no passphrase set). The privacy-banner is hidden for plaintext files (the red banner is the dominant signal). WIF text is `user-select: all` so users can manually copy via OS shortcut if they really need the raw value. **Show recovery key (in-app panel) is NOT a download path** — the existing manage-gate + Reveal acknowledgement is sufficient; no re-prompt at reveal-time, no Copy button on the displayed value. **Done-states across rotation flows:** MoveAddressModal and ChangePassphraseModal both render a two-button done state ("Download again" + "Got it") with copy that explains the file contains both keys. The download payload is captured in a `useRef` (MoveAddressModal: `combinedBackupRef`) or `useState` (ChangePassphraseModal: `doneBackup`) so the user can re-download without re-running the rotation. **Do not re-introduce Copy buttons on WIF surfaces** — exposing the raw key one keystroke from clipboard is the threat model the warnings are protecting against. **Do not pass filenames to `downloadBackup`** — the auto-builder is the single source of truth for the naming pattern.

- **[SUPERSEDED 2026-05-03] Unlock-UI shake on locked-action — global pointerdown catcher (settled 2026-05-02, replaced 2026-05-03).** Was: a global `LockedClickCatcher` listened for pointerdown capture-phase on every interactive element while locked, fired shake. Replaced because the catcher was action-agnostic — it shook for read-only interactions (opening menus, expanding sections, even just clicking the chip). User explicitly said reading posts and AI chat should NEVER trigger sign-in. Centered modal + per-handler `requireIdentity()` is the durable pattern. **Do not re-introduce a global click catcher.**

- **Currency display: protection-aware default + permanent user override (settled 2026-05-02) — ⚠️ SUPERSEDED 2026-06-26 (see "Currency default is ALWAYS dollars — no protection-aware auto-flip" above).** This entry described a protection-aware default that auto-flipped protected accounts to Goat (sats) with a one-time `GoatModeToast`, driven by a `setModeProgrammatically("goat")` effect, with the toggle stored at `localStorage["bsvibes_currency_mode"]` and the hook reading `bfn_keypair_enc` in its initializer. **ALL of that was REMOVED 2026-06-26** — the auto-flip, the `GoatModeToast` (file deleted), `setModeProgrammatically`, and the `bfn_keypair_enc` read no longer exist. Current reality: `useCurrencyMode` ALWAYS defaults to dollars (Noob); sats is opt-in via the toggle; an explicit choice (stored at `localStorage["opencook_currency_mode"]`) is honored forever. Kept here only as the record of the reversal.

- **GitHub link: pill tease + modal footer (settled 2026-05-04).** OpenCook surfaces its public repo (`github.com/Challotes/opencook`) via a two-layer design rather than a single header link or footer credit: (1) a decorative GitHub octocat (14x14) embedded inside the "Ask AI" pill AFTER the label, and (2) a centered link inside the AgentChat modal footer with the tagline *"The code is open."* + `↗` arrow. The pill icon is **decorative only** — clicking the pill opens the modal as it always did; the icon is a visual hint that there's something to discover, not a separate click target. The actual clickable repo link lives one level deeper in the modal footer where there's room for context. **Why this design beats the alternatives:** (a) a header GitHub icon next to the chip carries no meaning for the 3-second skimmer who hasn't yet read what OpenCook is — the link belongs to "any project" without context; (b) a peer GitHub icon next to the pill creates a button-next-to-button mental model where users misattribute clicks; (c) a manifesto-only link is contextually perfect but invisible to users who don't scroll that far; (d) a live star count widget at low star numbers actively signals "nobody cares yet" before the pitch lands; (e) embedding a clickable GitHub icon INSIDE the pill splits the pill into two tap targets and breaks the affordance — devs notice the icon, expect the pill click to navigate to the repo, and end up in a chat modal instead. The dual-surface design threads the needle: discoverable via the pill (which lives above the fold in the PostForm footer row), meaningful via the modal (where the tagline does persuasion work the bare icon can't). The icon is shown in BOTH normal and `highlight` states (highlight = amber pulse triggered when the user clicks "Chat with the agent" in the manifesto) because that's the highest-intent moment for the open-source signal — the user has just read the Vision pitch and is being directed to the pill. **Anti-patterns to avoid:** live star count widgets, "Star us on GitHub" CTAs, amber color on the icon (amber is reserved for brand accent + warnings), labels on the pill icon ("Source"/"GitHub" — competes with "Ask AI" and turns the pill into a nav bar), centered footer styled as a CTA-button (the link is a quiet trust signal, not a primary action). Tagline copy "The code is open." was selected over "Built in the open" (generic, every project says it) and "Open source by design — every fork proves this works" (too clever, too long for the modal context).

## Platform & Distribution

- **PWA over App Store, web-first only (settled 2026-05-10).** No iOS App Store or Google Play submission. Apple's 30% IAP cut is incompatible with sat-level boot fees, Apple has historically rejected BSV/crypto-payments apps, and UGC-with-payments triggers Apple's highest moderation tier. Months of submission with low success probability is a category cost we can't absorb. Google Play alone creates an Android-only third storage sandbox that worsens identity confusion. The PWA install flow IS the "app experience" on every platform. **Anti-patterns:** retrofitting boot through Apple IAP (destroys economics), Android-native first then iOS later (creates sandbox asymmetry), Capacitor / native-shell wrappers (still triggers IAP review). **Revisit only when:** all three are simultaneously true — major traction (>5k WAU), iOS PWA notifications still painful enough to lose users, AND a clear App Store policy path opens. Until then treat as settled. Full rationale in LAUNCH_PLAN.md D1.

- **In-app browsers → read-only live feed + detection-independent value-gate (FINAL — 2026-06-29 round 3; supersedes all the server-splash history below).** In-app WebViews are detected **CLIENT-SIDE** — Telegram-iOS via `window.TelegramWebviewProxy` (its UA is byte-identical to Safari, confirmed on the owner's device via inappdebugger.com, so the SERVER can't see it); X/Instagram/Facebook/… via UA tokens. When detected, the **live feed renders normally but is READ-ONLY**: scrolling/reading is free; any WRITE attempt opens `<InAppPromptModal>` ("open in your browser"). Coverage: post/boost/reboot inherit the gate via a read-only branch placed **FIRST in `requireIdentity()`** (before the identity-truthiness check — an in-app user HAS a harmless minted identity that would otherwise pass); the profile chip + "Add funds" are gated **explicitly** (they don't route through `requireIdentity()`). `isReadOnly = isInAppBrowserClient() && !detectStandalone()` — the `!detectStandalone()` term is **LOAD-BEARING** (installed iOS PWAs also drop `Safari/`, so without it they'd be wrongly locked read-only). **The hard funds floor is DETECTION-INDEPENDENT:** `FundAddress` hides the deposit QR/address behind a "Save your account first" panel until `backedUp` — so real money can never land on an unrecoverable key, *even if detection fails completely*. Earnings are covered transitively (no write → no posting → no earnings) plus the existing `FirstEarningToast` save-nudge. The harmless key mint **still happens** — the identity core is UNTOUCHED (no lazy-identity, no mint surgery); the brief feed-flash before read-only engages is cosmetic because the value-gate is the floor. `page.tsx` is back to **static/ISR** (client-side detection → reclaimed edge caching for link-preview crawlers). Misdetect escape: `dismissReadOnly()` persists `opencook_inapp_continue` in `sessionStorage`. **Framing = defense-in-depth:** client detection = UX (the read-only nudge), the `backedUp` value-gate = the funds floor; a detection miss degrades UX, never loses funds. Industry-aligned (Google blocks OAuth in embedded WebViews; PayPal/Amazon warn+redirect — "allow browsing, gate the value action"). Files: `isInAppBrowserClient()` (`in-app-browser.ts`), `isReadOnly`/`inAppPrompt`/`requireIdentity` branch (`IdentityContext`), `InAppPromptModal`, chip+Add-funds gates (`IdentityBar`), value-gate (`FundAddress`). **Threat note:** a key born in an in-app WebView is inherently untrusted (the host app *could* read it) — but that's a universal in-app-browser truth no web app can fix; the realistic risk is storage WIPE (key lost → funds stranded), which the value-gate closes. **↓ SUPERSEDED HISTORY below — the server-side "splash with a window" + fail-safe-allowlist rounds relied on SERVER-side UA detection, which is byte-identical to Safari for Telegram-iOS so it could never see it; `InAppBrowserSplash`/`InAppStandaloneGuard` are DELETED and `page.tsx` is reverted. Kept for the decision trail. ↓** Detect known in-app browsers (Facebook, Instagram, X, TikTok, LinkedIn, WeChat, Line, Discord, Snapchat, Pinterest, Reddit, Slack, Telegram, KakaoTalk) at the root layout and serve a splash with logo + headline *"Open OpenCook in your browser"* + body *"You're inside [App]'s built-in browser. OpenCook uses your browser's secure storage. Open in your browser instead — your account stays with you."* **REVISED 2026-06-29 → "splash with a window" (content-first), superseding the original hard-block stance.** The splash is NOT a dead-end: it shows a **static, server-rendered preview of the top feed posts** alongside the CTA, so a visitor sees value before the ask. A 3-lens agent panel drove the change — *growth:* a cold wall bounces ~70-85% before any value is shown and kills link-sharing virality (a shared link that resolves to a wall makes the sharer look bad); *security:* it is **funds-safe by construction** — the splash renders in `page.tsx` BEFORE `<Feed>`/`IdentityProvider` mounts, so **no key is ever minted in an in-app session** (the phantom-identity risk is structurally impossible, not merely suppressed); *architecture:* the entire gate lives in `page.tsx` (one touched file), outside the just-stabilized identity core. The preview is strictly read-only (it imports NO boost/post/identity/QR surfaces) — the only actions are "open in your browser" + a `?continue=1` escape hatch for UA misdetection. The original *"read-only adds confusion when they tap post"* worry is moot: there are no inert post/boost buttons to mislead, only the browser-switch CTA. **Still a block at the door — just a door with a window.** (Original stance was a hard block with no read-only mode — reversed after the panel.) Search-engine crawlers (Googlebot, Twitterbot, etc.) bypass the splash so SEO/OG previews work. **Surface differentiation, not platform-branded copy:** Android shows a real `[ Open in browser ]` button that fires an intent link; iOS shows NO button (Apple won't allow programmatic redirect to Safari) — instead, a static inline tip *"Tap Share, then 'Open in Browser'"* with share icon. Same headline + body across both platforms. **Why it matters:** in-app browsers have isolated localStorage, sometimes wipe between sessions, and never support push notifications. Letting users in creates phantom identities they will lose. **Anti-patterns:** allow-listing only known-good browsers (false-positives on niche browsers), soft warning that lets through (creates phantom identities), JS redirects to Safari (Apple killed every reliable trick years ago). See LAUNCH_PLAN.md D2. **Build (2026-06-29):** detector `src/lib/in-app-browser.ts` (UA match + crawler-exempt, unit-tested) → `page.tsx` reads the `user-agent` header server-side and renders `<InAppBrowserSplash>` (server component, reuses the `getPosts()` data) instead of `<Feed>` unless `?continue=1`; the CTA is the client child `<InAppBrowserCta>` (copy-link / Android Chrome-intent / iOS instructions / continue-anyway). **Open-the-installed-PWA (researched 2026-06-29):** NO separate feature — on **Android** it's effectively free (the "Open in Chrome" CTA routes out of the webview and Android's WebAPK intent filter can land the user in their installed PWA); on **iOS** it's a hard platform limit (you cannot open an installed home-screen PWA from a link, and `getInstalledRelatedApps` is unsupported). Added `launch_handler` to the manifest as the one free win (`focus-existing`); `getInstalledRelatedApps` is Android-only and dead in webviews, so not relied on. **Fail-safe allowlist + Telegram-iOS lesson (round 2, same day, after real-device QA):** the first build keyed on a denylist and MISSED Telegram on iPhone — Telegram's iOS WebView sends a bare WKWebView UA with no app token and no `Safari/` suffix (a 4-yr-old open Telegram issue confirms they never added a UA token). So detection flipped to a **fail-safe allowlist:** an iOS UA with no real-browser token (`Safari/`/`CriOS`/`FxiOS`/`OPT`/…) → splash; empty UA → splash; Android `; wv)` → splash; `electron` added. This fails in the SAFE direction (unknown → splash, never the old unknown → mint), which supersedes the original anti-pattern note above about pure allow-listing (the `?continue=1` escape + the denylist-first ordering bound the false-positive cost). **iOS PWA collision:** installed iOS home-screen PWAs ALSO drop `Safari/` (identical UA to a bare WebView), so `InAppStandaloneGuard` (client `navigator.standalone`) redirects installed PWAs into the app — the server-side UA alone can't distinguish them. **The detection-independent `lazy-identity` model (never mint without an explicit "create" tap) remains the documented backstop** if detection ever proves insufficient (e.g. if Telegram later adds `Safari/`); not built now since the allowlist is far cheaper and touches no identity core. Auditor-blessed funds-safe by construction both rounds.

- **Mobile modals adopt the AgentChat bottom-sheet pattern (no library) (settled 2026-05-10).** Six modals (`SignInModal`, `IdentityBar` You modal, `MoveAddressModal`, `RestoreModal`, `ChangePassphraseModal`, `FundAddress`) restructure to bottom-sheet on mobile / centered on desktop using the **same Tailwind pattern already proven in `src/app/AgentChat.tsx:207`**: container `fixed inset-0 z-[…] flex items-end sm:items-center justify-center sm:p-4 pointer-events-none`, panel `w-full sm:max-w-{sm|md|lg} rounded-t-2xl sm:rounded-2xl pointer-events-auto animate-[slideUp_0.3s_ease-out]`. **Wizards** (`MoveAddressModal`, `RestoreModal`, `ChangePassphraseModal`) get full-height sheets via `min-h-[80vh] sm:min-h-0 overflow-y-auto`. **Single-step modals** (`SignInModal`, `FundAddress`) get the half-height treatment. **`IdentityBar` (You modal) needs `max-h-[92vh] overflow-y-auto`** — it's the tallest modal (earnings + chart + activity + balance + rows) and overflows on small phones without the height cap. All preserve their existing internal logic, including the locked-state You modal cross-fade (DECISIONS.md 2026-05-01). **Anti-patterns:** don't introduce vaul / react-modal-sheet / any external library — the in-house pattern works; don't break the desktop centered-popover pattern; don't break the locked-state cross-fade. See LAUNCH_PLAN.md D3 + per-modal class table.

- **QR device sync is a post-launch layer, not launch (settled 2026-05-10).** Recovery file remains the structural primitive for cross-device identity transfer. QR sync is a UX accelerator that sits on top, planned as Bucket 6 after launch. Cryptographic model: source device wraps its already-encrypted `bfn_keypair_enc` blob in a one-time AES-GCM envelope (random transfer key), uploads envelope to server with short ID, ID + transfer key go in the QR (key in URL fragment so server never sees it), envelope auto-deletes after 5 min, destination scans QR, decrypts envelope locally, prompts user for passphrase. Plaintext WIF NEVER leaves source device — two layers of encryption, three independent factors required for compromise (physical access to QR, network access ≤5 min, passphrase). **Plaintext-mode users blocked from sync** until they set a passphrase — forcing function for the right behavior. **Five required cryptographic deltas before code** (per code-auditor 2026-05-09): use raw-key AES-GCM helper not PBKDF2 wrapper; one-shot atomic delete-on-read on GET endpoint; 5-attempt cap on typed-code fallback (single most important security control); bind envelope to id + origin via AES-GCM AAD; destination validates inner blob shape + ships full JSON wrapper + prompts-with-auto-backup before overwrite. See LAUNCH_PLAN.md D7 + Bucket 6 for full spec.

- **Single-broadcaster proxy at launch with miner-agnostic result type (settled 2026-05-10).** The `/api/broadcast` proxy ships configured to use ONLY GorillaPool at launch — no TAAL fallback yet. **Architecturally critical guardrail:** the broadcast helper exposes a single `broadcast(rawTx)` function with miner selection encapsulated inside; the result type is `{ status, txid, code }` — miner-agnostic. **Don't leak `gorillapool_*` strings into call sites or status enums.** With this guardrail, the double-broadcast hazard (an ARC success-then-CDN-failure mistakenly triggering TAAL retry) never materializes because only one broadcaster exists, and adding TAAL later is a localized internal change. Without the guardrail, adding fallback later becomes a cross-cutting refactor. Trade-off accepted at launch: an ARC outage freezes posts/boots until recovery (small launch-window blast radius given GorillaPool's track record). **When TAAL is added later:** classification rules required — `200/201` → success; `5xx` with no body → fallback to TAAL; `5xx` WITH ARC error body containing `txid` → return success-shaped response (tx accepted before HTTP layer failed); on timeout, query primary's `/v1/tx/{txid}/status` for 2s grace before falling back. Proxy is byte-transparent on request body — does not parse or transform (BEEF ancestry passes through untouched). See LAUNCH_PLAN.md C1 for full spec.

- **Two protection helpers, not one (settled 2026-05-11).** Identity service exports two helpers with deliberately different semantics that look almost identical and will be tempting to collapse: `isIdentityEncrypted()` checks ONLY whether an encrypted store (`bfn_keypair_enc`) exists; `isEffectivelyProtected()` returns true ONLY if encrypted exists AND no plaintext key exists. The distinction matters because of the **interrupted-upgrade case** — if a passphrase upgrade crashed mid-flow before `commitUpgrade()` could remove the plaintext, both keys remain in localStorage. `getIdentity()` already correctly prefers the plaintext one in that case, so the user is *effectively unprotected* (plaintext is what's actually being used). **UI gating must use `isEffectivelyProtected()`**, NOT `isIdentityEncrypted()` — otherwise unprotected users get routed to a passphrase prompt for a passphrase they never successfully set up. `isIdentityEncrypted()` is reserved for the internal race-handling inside `getIdentity()` where the narrower presence-check semantics are required. **Do not collapse these into one function.** See `src/services/bsv/identity.ts` doc comment.

- **iCloud Keychain requires a username anchor on rotation forms (settled 2026-05-18).** iOS Safari's "Save Password?" / "Update Password?" sheet only fires when the form has a stable `autocomplete="username"` value that iCloud can use to key the credential lookup. Without it, iOS skips the save prompt entirely on subsequent rotations — the first save works (no prior entry to match), but every rotation after that is silently ignored because iOS can't tell whether this is a new credential or an update to an existing one. Verified on iPhone iOS 18 (user saw exactly one saved entry in Settings → Passwords for bsvibes.com, regardless of how many rotations were performed). **Fix:** wrap the password inputs in a `<form>` element with a hidden `<input type="text" autoComplete="username" value={identity.name} readOnly hidden />` as the first child. `identity.name` is stable across rotations (the on-chain migration protocol preserves the name), so iOS sees subsequent rotations as updates to the same credential and fires "Update Password?" with the new value. Source: WWDC 2018 session 204 + Apple Password AutoFill docs + WHATWG autocomplete spec. **Do not use a value that changes across rotations (e.g. the hint).** That would defeat the matching logic and create duplicate keychain entries on every rotation. **Do not omit the username field.** That returns iCloud to silent-fall-through behavior.

- **Block scope covers both pagehide AND visibilitychange (settled 2026-05-18).** E24 introduced `blockSessionClear()` to suppress `pagehide → clearSessionCaches()` during iOS system sheets on PWA. **It was incomplete.** The iOS Save Password sheet (and Share, Files picker, etc.) fires BOTH `pagehide` AND `visibilitychange→hidden` on a standalone PWA. The `visibilitychange` handler in `IdentityBar.tsx` (which sets `manageAuthed=false` to destroy the manage gate on tab blur) was unprotected by E24 and fired during the iOS sheet, cascading through React re-renders and effectively closing the rotation modal mid-flow. **Fix (E26):** IdentityContext exposes `isSessionClearBlocked(): boolean` reading the same ref-counted block. IdentityBar's visibilitychange handler short-circuits when the block is active. One ref, two consumers — keeps the suppression surface coherent. **Any future visibility-driven teardown anywhere in the app must read `isSessionClearBlocked()` first.** Otherwise we'll re-introduce this class of bug.

- **Local state commits BEFORE parent notification in rotation/restore flows (settled 2026-05-18).** In React 19.2 + React Compiler, when a child component (like MoveAddressModal or RestoreModal) needs to render a "done" state AND notify the parent that identity has changed, the call order matters. Calling `onComplete(newIdentity)` first triggers `updateIdentity()` in the parent, which causes a parent re-render BEFORE the child's local `setStage("done")` / `setImportSuccess(true)` commits. The reconciliation can unmount the child before the done state renders. **Fix:** always commit local done-state FIRST, then notify parent. In MoveAddressModal `runRecording()`: `setStage("done")` → `onComplete(result.identity)`. In RestoreModal `performImport()`: `setImportSuccess(true)` → `onSuccess(imported)`. The bug-class generalizes — any modal that has its own dismissal/done state AND fires a state-mutating callback to the parent must commit its own state first. **Do not reverse this order.**

- **No auto-close timers on success states in restore / rotation modals (settled 2026-05-18).** RestoreModal previously had `setTimeout(() => { onSuccess(); handleClose(); }, 1200)` — fired 1.2s after success regardless of any iOS sheet timing or user attention. This caused the modal to close before users could see the confirmation that their restore had succeeded, especially on PWA where the iOS Save Password sheet sat on top. **Fix:** all rotation/restore success states transition to an explicit "done" step with a user-dismissable button (Got it / Continue). Parity with ChangePassphraseModal / MoveAddressModal done-states. **Do not re-introduce auto-close timers** — they always race with iOS system sheets and either confuse the user or hide critical confirmation UI.

- **Restore-from-encrypted-file adopts the file's passphrase as the new identity's passphrase (settled 2026-05-22).** When a user restores from a passphrase-protected recovery file, the passphrase they typed to decrypt the file becomes the passphrase guarding their new identity going forward. The file's hint is preserved too. **Fix:** `importEncryptedIdentity(wif, passphrase, name?, hint?)` in `identity.ts` re-encrypts the restored WIF and writes it to `bfn_keypair_enc`. Mirrors `upgradeIdentity`'s store shape so `getStoredAnonName` / `getStoredHint` continue to work. Session caches (`_sessionIdentity` etc.) are primed inside the function so `signPost` (used immediately by `cleanupMigrations`) succeeds without forcing the user to re-unlock — the passphrase was JUST provided. **Behavior before this:** restore wrote plaintext via `importIdentity`, discarding the typed passphrase. User had to rotate again to be protected. The "passphrase adopted on restore" pattern was NEVER in git history before this — confirmed via `git log --all -S "encryptWif"`. Do not assume it's a regression restoration; it's a new feature. **Do not collapse `importIdentity` and `importEncryptedIdentity` into one function** — they have distinct semantics (plaintext write vs encrypted write).

- **No auto-download on rotation; explicit Save with stakes context (settled 2026-05-22).** Pre-E27 rotation auto-downloaded a recovery file the moment `runRecording` completed. This produced UX friction (iOS PWA full-page download sheet on every rotation, multiple files piling up in Downloads with similar names) and an authenticity problem (`markBackedUp()` fired automatically, falsely claiming the user had saved when iOS may have actually dropped the download). **Fix:** `MoveAddressModal.runRecording` no longer calls `downloadBackup`. The combined backup payload is held in `combinedBackupRef`. The done-state UI is now a context card showing the user's earnings (sats + USD via `useBsvPrice`) with an explicit "Save recovery file" primary button and "I'll do it later" secondary link. Tapping Save uses the Web Share API; tapping Skip leaves the new key without an external file but the amber "Unsaved key" warning dot persists in IdentityBar via the per-address saved flag. **Disaster-recovery preserved:** pre-rotation file STILL auto-emits on failure mid-flight (the safety net is in the failure path, not the happy path). **Do not re-add the auto-download in `runRecording`.** **Do not call `markBackedUp()` from `onComplete` (rotation reached done stage)** — it must only fire after the user actually saves. Use the new `onSaved` callback prop on MoveAddressModal for that signal.

- **Web Share API for recovery files; AbortError is a deliberate cancel (settled 2026-05-22).** `shareOrDownloadBackup(data)` in `backup-template.ts` is the new save path. It uses `navigator.share({ files: [file] })` when available, giving iOS users the familiar rounded share drawer with "Save to Files" as a top-level option, instead of the intrusive full-screen download sheet. Implementation constraints (per Web Share API spec + community reports):
  - **MIME type is `text/html`** (E28a, 2026-05-25 — superseded the earlier E27 decision to use `application/octet-stream`). E27 chose octet-stream based on a researcher's "iOS treats text/html as hostile" guidance, but iPhone PWA testing showed the share drawer NEVER appeared at all — silent fallback to `<a download>` every time. Current research + community consensus: WebKit's PWA process uses a stricter file-MIME allow-list than the Safari tab process, and `application/octet-stream` is OFF the PWA list while `text/html` is ON it. Diagnostic logs added in E28a will confirm; will revert if disproven. **Do not flip back to octet-stream without first proving via diagnostic logs that text/html is the cause of share failure**, since the share drawer working on PWA is one of the central UX wins of E27.
  - **Build the `File` object SYNCHRONOUSLY inside the click handler before any `await`** — iOS Safari's transient activation token expires across async boundaries. The function does the entire HTML generation + File construction sync before calling `navigator.share`. Callers must NOT `await encryptWif` or similar between the click and invoking the helper; pre-build any encrypted payload in a `useEffect` and stash it in state.
  - **`AbortError` is the user dismissing the share drawer.** Return `{shared: false, cancelled: true}` and DO NOT fall back to `<a download>` — that would re-trigger the intrusive download sheet the user just deliberately closed. UX regression.
  - **Other share errors fall back to `<a download>` via the legacy `downloadBackup`.** `canShare` can return true and `share` still throw — always wrap in try/catch.
  - **Caller is responsible for `blockSessionClear`** — the iOS share sheet fires `pagehide` and `visibilitychange→hidden` like any other system sheet, so callers should hold the session-clear block across the share. See `MoveAddressModal.handleSaveBackup` and `RestoreModal.handleSaveOldKey` for the pattern.

- **Per-address saved flag — global backedUp + per-address bsvibes_saved:&lt;addr6&gt; (settled 2026-05-22).** Pre-E27 a single global `bsvibes_identity_backed_up` boolean tracked "has the user ever saved a recovery file." Post-E27 we layer a per-address flag (`bsvibes_saved:<addr6>`, ISO date) on top. Each rotation creates a new address; the global flag remains true (the user has SOME saved file in their history) but the new address is freshly unsaved. The amber "Unsaved key" warning dot in `IdentityBar` reads `backedUp === false || !isAddressSaved(identity.address)` — either condition surfaces the dot. **The global flag MUST ONLY flip on explicit user save** — moved out of `MoveAddressModal.onComplete` and into the new `onSaved` callback that fires from `handleSaveBackup` after successful share. Per-address keys also written by RestoreModal `performImport` (because the file the user just restored IS the backup) and by all existing `markBackedUp()` call sites (Save/Copy/Show key paths in IdentityBar). **Do not collapse the two flags** — they have distinct semantics.

- **RestoreModal restore-pre is Skip-with-confirmation-toggle (settled 2026-05-22).** Pre-E27, when a user restored to a new identity, the modal auto-downloaded their OUTGOING identity's recovery file before swapping (defense-in-depth). Post-E27, this is a user choice: a context card shows the outgoing identity's stats + Save (primary) and Skip (secondary). Tapping Skip transitions to a red warning state requiring a second tap on "Skip & restore anyway." Force-save was rejected — users with their backup already saved elsewhere (in a password manager, another file, etc.) shouldn't be forced to re-download. The two-step skip provides informed-consent without total prevention. **Security note:** unprotected identities with no externalised key WILL be permanently lost if the user Skips. Acceptable because the user explicitly confirmed the warning. **Do not collapse the two-step Skip into a single button** — the friction is the consent gate.

- **Restore of rotated keys is blocked outright (settled 2026-05-26, E29 — "Design C-strict").** Any recovery file whose key has a forward migration record on-chain CANNOT be restored as an active identity. The on-chain migration is treated as a permanent revocation event — like Google / Apple invalidating device tokens on a password change. This closes a critical security vector: every OpenCook user's FIRST identity is plaintext by default (no passphrase until they upgrade), so the plaintext recovery file is a non-revocable leak vector. If restore-then-reclaim were allowed (the previous behavior via `cleanupMigrations` auto-rewrite), anyone who ever obtained the plaintext file (laptop theft, email leak, cloud trash, screenshot) could later take over the user's future earnings — even years after they upgraded to a strong passphrase. The block uses the migration record as the only source of truth: once a user rotates, the old key is permanently retired from OpenCook's UI perspective.

  **Implementation:** new read-only endpoint `GET /api/restore-eligibility?pubkey=<hex>` returns `{ allowed: true }` if no forward migration exists, else `{ allowed: false, rotatedAt, newAddrPrefix }`. Rate-limited 30/min/IP. The check fires BEFORE any identity-state write in both `RestoreModal.doImport` and `HomeScreenWelcomeGate` (both `handleFile` plaintext branch + `handlePassphrase` encrypted branch). Fail-safe: any network/parse failure during the check ALSO blocks — without verification we can't safely allow the restore.

  **The error UX is non-blocking** — the user is told their key was rotated, given the date + new address prefix, and pointed at finding their most-recent recovery file. They can also use the WIF in an external BSV wallet (Yours, Centi, Sparrow) to spend any UTXOs at the old address — OpenCook is not a custodian of those funds.

  **Trade-off accepted:** users who lost their newer key and only have a pre-rotation file cannot recover via OpenCook UI. Mitigated by the existing combined-recovery-file pattern: every rotation file contains BOTH keys under one passphrase, so only the FIRST plaintext save (before any rotation) is lost-recovery-blocked.

  **`cleanupMigrations` server action REMOVED in E31 (settled 2026-06-01).** Originally added 2026-03-28 (commit `31a9d92`) to fix a specific bug: a user could rotate A→B, lose B, re-import A, and then have every future payout silently redirect to the (now lost) B address. Re-importing A used to be permitted; `cleanupMigrations` deleted the stale A→B migration row so payouts flowed to A again. **E29 (2026-05-26) made that scenario structurally impossible** by blocking re-import of any rotated key — so the function's only valid call site (RestoreModal's `doImport`) was already removed, and the underlying bug it fixed can no longer occur. The function then sat orphaned in `actions.ts` with a documented retention rationale ("future signature-gated admin reclaim design") that didn't survive scrutiny: that future design would need different auth (admin pubkey, not the from-key holder's WIF), different message format, and an audit log — making the existing 75 LOC reference material at best, not literal reuse. With E31 closing the rotate-from-stale takeover, keeping `cleanupMigrations` as a guarded-but-still-callable server action was the worst of both worlds (dead code that's still importable). **Recoverable from commit `31a9d92` (`git show 31a9d92:src/app/actions.ts`) if a future admin-reclaim feature ever materialises.**

  **DO NOT add a UI path that allows restoring rotated keys**, even with a warning. The previous architect-recommended "warn + opt-in reclaim" hybrid was rejected — it left the same plaintext-key attack vector open, just behind a click the attacker happily makes. **DO NOT re-introduce `cleanupMigrations` without an admin-signature auth model.** **DO NOT remove the migration-record check** — the security model depends on it.

  Edge case: a key previously reclaimed via the legacy `cleanupMigrations` auto-bridge AND THEN re-rotated will be permanently blocked from restore. Acceptable — user can recover their UTXOs via external wallet; rotation forward is irreversible by design.

- **E30 stale-key session-lockout (settled 2026-05-28, "UI-layer only").** When a device holds a key that has been rotated forward on-chain (typically: user rotated on another device and then opens this one), the app surfaces a `<StaleKeyModal>` on first detection and replaces the textarea in `PostForm` with an amber banner blocking the compose flow. This is a UI-layer lock — `createPost` / `bootPost` server actions are NOT modified and still accept signatures from the old key (which is the deliberate trade-off; see SECURITY_AUDIT.md L7).

  **Chosen approach:** detection via polling. `useFeedPolling` sends the local pubkey in an `x-bsvibes-pubkey` request header on every `/api/posts` poll (header, not query string — keeps pubkey↔IP correlation out of CDN/proxy URL access logs). The server endpoint returns `key_status: { stale: true }` only when ALL of (a) `process.env.E30_STALE_KEY_ENABLED === "true"`, (b) the header passes shape validation, (c) a forward-migration row exists. Any other condition omits the field. Client treats anything other than strict-equal `{ stale: true }` as not-stale (F1+F2 fail-open — a server bug must never mass-lock users).

  **State machine:** new `kind: "staleKey"` variant on `IdentityState` (`useIdentity.ts`). The underlying identity is retained in `staleKey` state so the chip can show the cached anon name and non-signing reads (address, name, pubkey) keep working; mutation handlers gate via `requireIdentity()`, which now branches: stale → `openStaleKeyModal()` instead of `openSignIn()`. The context wrapper `markIdentityStale` is suppressed when `isSessionClearBlocked()` is true (F3 mitigation — prevents self-stale during own-device rotation when MoveAddressModal/ChangePassphraseModal hold the block).

  **Reasoning for UI-only vs per-mutation server gating:** open/closed principle. E29's `/api/restore-eligibility` is already proven; E30 reuses the same `getForwardMigration` helper for the polling check and the existing `requireIdentity()` Hard Rule #7 universal pattern for the gate. Any future mutation feature (boost, tip, etc.) inherits the lock automatically — no per-feature server changes needed. Lower LOC, no new server attack surface. A malicious WIF holder bypassing the UI entirely (direct BSV broadcast) is documented as L7 residual risk with a clear retreat path (add `getForwardMigration` lookup to `createPost`).

  **U1 escape hatch** ("I don't have the newer file"): inline expand-below within `<StaleKeyModal>`. Tells the user honestly that earnings + posting follow the newer key, on-chain history is intact, the older key on this device can no longer post or earn. No "support" hook, no recovery promise, no "start fresh" path. Acceptable per the threat model — frequency is low (rotated + lost newer file + still has older device).

  **R1 fix:** IdentityBar Show Recovery Key panel subscribes to `staleKey` and force-closes the dropdown on transition — otherwise a user mid-reveal could photograph/copy a now-dead WIF.

  **Auto-open behavior:** the StaleKeyModal auto-opens on the FIRST `ready → staleKey` transition (via a ref-guarded effect in IdentityContext). Subsequent polls that re-fire `markIdentityStale()` are no-ops at the hook level (already-stale state), so a user-dismissed modal does NOT spontaneously reopen. The amber banner is the persistent re-entry point.

  **Z-index stacking:** SignInModal z-[80], StaleKeyModal z-[90], RestoreModal z-[100]. RestoreModal was bumped from z-[70] in E30a so the restore flow can stack cleanly on top when chained from the stale modal.

  **Pre-rotation pubkey backfill:** `Identity` type gained a required `pubkey: string` field in E30a; `materializeFromStored` derives it on load for pre-E30a `StoredIdentity` payloads and rewrites the localStorage entry so subsequent loads skip the SDK round-trip. Backfill is best-effort (write failures don't block return).

  **Retreat path:** if attribution griefing is observed (L7 escalates), add a `getForwardMigration` check inside `createPost` server action and reject — one-session change. UI gating stays as defense-in-depth, server gating adds the second layer.

  **DO NOT remove the F3 guard** in `IdentityContext.markIdentityStale` — it prevents self-stale during own-device rotation. **DO NOT remove the fail-open posture** in `shouldCheckStaleness` / `useFeedPolling` — strict-equal `{ stale: true }` is the only signal that triggers the lock. **DO NOT switch pubkey from header to query string** — the privacy concern is real even though pubkeys are public on-chain (header keeps the correlation out of default proxy logs).

- **E31 block rotate-from-stale (settled 2026-06-01, "symmetric to E29").** Discovered during E30 manual testing: a stale-key holder could still call `migrateIdentity` to rotate their key. Old WIF signs a valid migration → server accepts (signature is cryptographically correct) → `INSERT OR REPLACE` on `migrations` table silently overwrites the legitimate rotation → chain head moves to the attacker's new key → legitimate current key holder gets locked out. Same attack class as E29 (which closed restore-from-stale), just at a different endpoint.

  **Server-side fix:** `migrateIdentity` now calls `getForwardMigration(oldPubkey)` after signature verification and rejects with `reason: "stale_key"` if a forward migration row exists. Same pattern as E29's `/api/restore-eligibility` check. Fails CLOSED on DB lookup errors (unlike E30's poll-time fail-open posture — re-opening the rotate-from-stale bug to a partial DB outage would defeat the purpose).

  **Client-side preflight:** both `MoveAddressModal.runCreating()` and `ChangePassphraseModal.handleChange()` now call `/api/restore-eligibility` BEFORE invoking `upgradeIdentity` (which runs the sweep). Without the preflight, the sweep would move funds to a new address that the server-side `migrateIdentity` then rejects — leaving funds at an address the server can't recognise (user still has the new WIF so funds aren't lost, but the OpenCook UI state is broken). Reuses E29's endpoint — same semantic question ("is this pubkey eligible to be the active key going forward?").

  **MoveAddressModal return-value check:** previously `await migrateIdentity(...)` was fire-and-forget — same class regression as historical BUG-10 (fixed in ChangePassphraseModal but never patched in MoveAddressModal when the latter was introduced). E31 fixes this: result is checked, stale-key reason surfaces specific copy, other failures throw generically.

  **UI trigger guard:** `IdentityBar.openMoveModal` now intercepts when `staleKey === true` and calls `openStaleKeyModal()` instead of mounting the rotation wizard. Three call sites feed through `openMoveModal` (Passphrase row, Not Protected red banner, manage-gate fallback) — guarding once covers all of them. Defense-in-depth on top of the server gate + client preflight.

  **Decisions made during design:**
  - Hard-lockout gate (replace feed with full-screen restore prompt) was considered and rejected — both UX and architecture agents independently recommended against. Reasons: punishes the legitimate multi-device case, removes earnings/feed visibility while stale, removes the persistent amber banner as a re-entry signal, would require relocating `useFeedPolling` outside FeedContent to avoid polling-stops-while-stale. The "stale-aware surfaces" count is 4-5 (well under the inflection point of ~6 where hard-gate becomes the right call). E30's modal+banner pattern stays.
  - `cleanupMigrations` deleted entirely rather than guarded (see updated entry above).

  **DO NOT remove the server-side `getForwardMigration` guard** in `migrateIdentity` — the entire E31 fix depends on it. **DO NOT switch the server-side check to fail-OPEN on DB errors** — that re-opens the takeover. **DO NOT remove the client-side preflight** — without it the sweep can run before the server rejects, stranding funds at an address the server doesn't recognise.

- **No `title` field in `navigator.share` when sharing files (settled 2026-05-25).** Earlier E27 implementation passed `navigator.share({ files: [file], title: "OpenCook recovery key" })`. iPhone Safari testing showed this saves the HTML recovery file AND a sidecar `.txt` file containing just the title string "OpenCook recovery key". Known iOS Web Share API quirk: when both `files` and `title` are present, iOS treats them as separate share items. **Fix:** share files only — drop the `title` field. **Do not re-add `title` to file-sharing calls.** If a label is needed for the user, communicate it in the UI before invoking share, not in the share payload itself.

- **iOS Quick Look noscript / input-readonly pattern (settled 2026-05-18).** Two iOS-Quick-Look-specific quirks force the recovery file HTML to be written in a non-obvious way that contributors will be tempted to "fix" back: (1) `<noscript>` content does NOT render in iOS Quick Look even though no JavaScript executes. The HTML spec says `<noscript>` renders when the *browser engine* reports scripting as "disabled," but Quick Look's sandboxed WebKit still reports scripting "enabled" at engine level — it just never runs scripts. So `<noscript>` content stays hidden. **Solution:** use a normal `<div id="quicklook-notice">` visible by default, hidden by a tiny JS IIFE on load. The "JS-required" warning shows in Quick Look (no JS) and disappears in real browsers (JS runs and hides it). (2) `user-select: all` is unreliable in iOS Quick Look — the preview UI intercepts touches at a layer above CSS, so long-press-to-copy on a `<div>` or `<span>` doesn't fire. **Solution:** use `<input type="text" readonly>` for the address row and `<textarea readonly>` for the WIF — native form controls retain OS-level tap-to-select / long-press handles in Quick Look. Pattern used by 1Password Emergency Kit, Bitwarden emergency sheet. **Do not revert to `<noscript>`. Do not switch addresses or WIFs back to spans/divs.** CSS strips native form-control defaults so inputs look identical to today's spans. `copyText()` reads `el.value` for form controls and `el.textContent` for spans (the "Saved" date row is still a span). Verified by the researcher agent against MDN spec + community reports + Apple's Quick Look docs (which don't explicitly document this behavior, but the spec mechanism + industry consensus is consistent).

- **iOS Permissions API for "microphone" is unreliable; call recognition.start() directly (settled 2026-05-16).** Tested on iPhone Safari: `navigator.permissions.query({ name: "microphone" })` returns a cached `denied` state long after the user re-enables mic access in Settings → Safari → Microphone. The cache only refreshes on hard refresh / app reinstall. **Don't gate `recognition.start()` behind a permissions pre-check** — it surfaces the native iOS prompt on first use and the existing `onerror` handler catches genuine `not-allowed` errors. A previous version of `PostForm.tsx` had this pre-check and it caused our "Enable in Settings → Safari → Microphone" toast to fire forever even with permission granted. The pre-check is both redundant and broken — recognition.start() is the single source of truth.

- **autoComplete attributes are required on all password inputs (settled 2026-05-16).** iOS 17+ Safari and iCloud Keychain only trigger the "Save Password?" prompt when `<input type="password">` carries the correct `autoComplete` value. **Use `autoComplete="current-password"`** on unlock paths (SignInModal, IdentityBar manage gate, ChangePassphraseModal verify step) and **`autoComplete="new-password"`** on rotation paths (ChangePassphraseModal new + confirm, MoveAddressModal new + confirm). Without these, Safari treats the field as a generic password input and skips the save prompt — iCloud Keychain never stores the passphrase. Verified: a Safari user reported the save prompt had silently stopped working; root cause was that none of our 7 password inputs had `autoComplete`. **Any new password input must carry an autoComplete attribute.**

- **Pagehide-driven session clear must be blockable during rotation flows (settled 2026-05-16).** `IdentityProvider` listens for `pagehide` in standalone PWA mode and calls `clearSessionCaches()` — the intentional password-manager-style cleanup that fires when the user really backgrounds the app. iOS also fires `pagehide` on its own *system sheets* — Save Password, Share, Files picker — which is NOT a real backgrounding event but our handler can't tell the difference. Without protection, the iOS Save-Password sheet shown after a passphrase rotation would torch the in-memory session and silently unmount the modal before the user saw "Download again / Got it". **Solution:** ref-counted `blockSessionClear()` / `unblockSessionClear()` on IdentityContext. The pagehide handler checks the ref before clearing. ChangePassphraseModal blocks at `handleChange` entry and unblocks in `handleClose`; MoveAddressModal blocks at `runCreating` entry and unblocks via a wrapped `onClose` funnelling every dismissal path. Both have a useEffect cleanup as a mid-flow unmount safety net. **Any future flow that triggers an iOS system sheet during sensitive identity state must wrap itself in block/unblock.**

- **Welcome gate fires on standalone-mode + no identity, four real causes (settled 2026-05-10).** When the app opens in PWA standalone mode AND no identity exists in localStorage, the welcome gate fires asking the user to restore from a recovery file or start fresh. The four real causes that produce this state, all of which need honest copy: (a) **iOS 7-day ITP eviction** — Apple's automatic privacy feature wipes site data after ~7 days of no visits, even from "properly installed" Safari PWAs; (b) **iOS Private Browsing install** — storage is ephemeral, gate fires every cold open (rare); (c) **Non-Safari iOS install** (Brave/Chrome iOS) — different storage sandbox from Safari; (d) **Fresh install / manual storage wipe**. **Copy must be honest in all four cases:** *"We couldn't find your identity on this device"* — reads as a normal restoration prompt, not a system error. **Don't promise** *"you only see this if you installed from a non-Safari browser"* — would read as a bug when ITP eviction kicks in. **Implementation guardrail:** synchronous pre-hydration check in `IdentityProvider`'s lazy initializer must defer auto-key-generation until welcome-gate decision; race condition otherwise where IdentityProvider auto-generates a fresh key before WelcomeGate mounts. Drop the "first-launch flag" entirely (would persist across storage wipes and cause gate-fires-only-once-ever). See LAUNCH_PLAN.md C3, #3, #5.

- **Save → Install → Welcome-gate → Notifications is one ordered flow (settled 2026-05-10).** The trigger sequence is: (1) silent identity creation on first visit — no nags; (2) after meaningful action (first earning, first boot, or explicit "Save" tap), the save flow produces a recovery file; (3) ONLY THEN does the install pitch fire; (4) user installs; (5) on the first home-screen open the welcome gate resolves identity; (6) notification permission is requested after the gate completes. **Ordering rule:** the install pitch MUST NOT fire until all four conditions hold: `recoveryFileExists() && !isStandaloneMode() && isSupportedInstallPlatform() && !installPitchDismissedRecently()`. This logic lives in `src/lib/install-pitch.ts`. Implemented in `src/components/InstallPitch.tsx`, `src/contexts/InstallContext.tsx`. **Why the order matters:** on iOS, installing creates a new storage sandbox that does not share localStorage with the browser tab that existed before install. If the install pitch fires before a recovery file exists, the user installs, lands in a fresh sandbox with no identity, and has nothing to restore from. The ordering guarantee means any user who reaches the install pitch has a recovery file and therefore a working restoration path. **Do not fire the install pitch on first visit**, on any visit before a file exists, or after "more secure" framing — install does not improve security, only convenience and privacy. **Do not remove or relax the `recoveryFileExists()` gate** without a replacement that provides equivalent restoration confidence. See LAUNCH_PLAN.md D4, Status #10.

- **Welcome-gate copy uses intent, not technical state (settled 2026-05-10).** The home-screen first-launch screen is: header *"Welcome back or starting fresh?"*, body sentence *"We couldn't find your identity on this device."*, primary button *"Restore from your saved file"* (sub-text: *"Use your most recent recovery file. Your posts and earnings come back."*), secondary button *"Start with a new identity"* (sub-text: *"Begin fresh on this device. You can save and restore later."*). This copy was arrived at after deliberate review — **do not replace it with technical-state descriptions.** Shipped in `src/components/HomeScreenWelcomeGate.tsx`. **Why intent-framing:** users have no reason to understand iOS storage sandbox mechanics, ITP eviction, or PWA install isolation. "Returning or starting fresh?" maps cleanly to every real cause that lands a user on this screen (non-Safari install, ITP eviction, Private Browsing install, manual wipe) without needing per-cause branching. Auto-detecting the "right" choice creates false confidence — the user knows their intent better than a localStorage heuristic. "Start fresh" must not feel penalised; it's a valid choice for new-device-first-run users. The "Scan from another device" tertiary option is appended here once Bucket 6 QR sync ships. **Do not** auto-select a path, add technical-state language like "if your address has changed…", or make "Start fresh" feel scary. See LAUNCH_PLAN.md D5, Status #3, #5, #9.

- **Service worker scope discipline: push-only, no fetch handler (settled 2026-05-10).** *(Forward-looking guardrail — the SW does not exist yet. `public/sw.js` is NOT in the repo as of 2026-06-04. This entry locks the design for when Bucket 3b lands.)* The OpenCook service worker (Bucket 3b) is **push-only**. It registers a push subscription, handles `push` events, and nothing else. **There is no `fetch` event listener. There are no caching strategies. There is no offline support.** The app requires a live network connection to read the chain and SQLite — offline mode is architecturally incoherent for this product. Caching strategies in a Turbopack dev environment produce non-deterministic SW behavior that is hard to debug. **Do not add a fetch handler to the service worker**, even for static assets. Do not add `workbox`, `sw-precache`, or any library that implies caching. Do not treat the SW as a background-sync surface for posting; that belongs in a dedicated Phase 7 architecture decision. The SW file is `public/sw.js` and must be registered from a CLIENT component (not from a server layout boundary — see C5 entry). CSP requires `worker-src 'self'`. **The one exception:** a minimal `install`/`activate` lifecycle handler to claim clients is acceptable and expected. If a PR adds ANY `fetch` or `cache` operation to the SW without an explicit supersession of this decision, treat it as an accidental regression. See LAUNCH_PLAN.md D6, C5.

- **First earning toast trigger wires to `/api/earnings` polling, not to `boot-confirm` (settled 2026-05-11, architect's refinement).** The "You just earned your first sats. Save your recovery file" toast fires from the client-side earnings poll. On each 30-second `/api/earnings` response: if `total_sats > 0` AND the `bsvibes_first_earning_save_dismissed_until` localStorage timestamp is either unset OR in the past → fire toast → set timestamp to `now + 48h` (regardless of whether the user taps Save or Later). Copy: *"You just earned your first sats. Save your recovery file — if you lose this device without it, they're gone."* Primary button: **Save now**. Secondary: **Later** (48-hour suppression, not 30-day — this is the highest-stakes save moment). **Note (2026-06-03 audit):** the suppression is a 48-hour timer, NOT a one-shot flag. The toast CAN re-fire after 48h if the user hasn't saved yet — that's intentional, since the amber "Unsaved key" dot in IdentityBar is the persistent fallback signal between re-fires. **Do not wire this to the `/api/boot-confirm` route.** `boot-confirm` is Bucket 4's territory — wiring a UI trigger directly to it creates rework when Bucket 4 lands with the `publishPayout()` helper. The poll path is server-emit-site-agnostic: when Bucket 4 adds SSE/push via `publishPayout()`, the first-earning toast code changes nothing. The 30-second detection latency is acceptable for a save prompt — this is not a real-time signal. The flag is set whether Save or Later is tapped, so a failed download never re-fires the toast automatically in the same session; the amber "Unsaved key" dot in IdentityBar is the persistent fallback signal. See LAUNCH_PLAN.md Status #4, sequencing revision section.

- **Notification copy discipline: single-line present-value, no roadmap hedging (settled 2026-05-10).** *(Forward-looking — push notifications and the `NotificationPrompt` component are Bucket 3b and not yet built. This entry locks the copy for when the component lands. The "install pitch text" surface IS live and follows this rule today.)* Push notification permission prompts and install pitch text both use the identical copy: *"Get notified when you earn."* One line. No listing of activity types. No "and more coming soon." No "Get notified when you earn, when your post is featured, or when the community responds." **Why single-line:** the marketer's review found that leading with a list of future triggers implicitly reads as "we'll spam you with things that aren't valuable yet." Present-value framing ("when you earn") is honest for today's implementation and broadens naturally as new triggers ship — the copy does not need to change when boot-received notifications or daily summaries land. The prohibition applies to both surfaces: the in-modal `NotificationPrompt` component and any inline install pitch copy. **Do not expand this string** without explicit product sign-off that the additional trigger is already live and provides clear user value. A promise of future notifications in a permission prompt that then goes quiet is worse than under-promising. See LAUNCH_PLAN.md Status #6, #10.

- **Install pitch surfaces — no timer-based dismissal (revised 2026-06-03, supersedes 2026-05-10).** The install pitch has three surfaces, each appearing only when the 5-condition `shouldShowInstallPitch` gate passes (`backedUp && protected && !standalone && installType supported && !engaged`):
  - **Slide-up sheet** (`InstallPitch variant="banner"`) — auto-fires once per tab session ~800ms after the gate opens. Full-screen modal with backdrop, 64px app icon, "Get the APP experience" headline, platform-appropriate CTA. **No X button** — only a chevron-down at the top center that minimises to the bookmark. Backdrop click also minimises.
  - **Bookmark** (`InstallBookmark`) — a bare 30px OpenCook app icon (34×34 tap target, no border — removed 2026-06-26; drop-shadow amber glow on highlight) in PostForm's footer row (centered between helper text and Ask AI pill). Visible when `installSheetMode === "bookmark"` — i.e., after the user minimised the sheet via chevron, OR on subsequent tab sessions (sessionStorage flag set on first sheet appearance). Tap re-opens the sheet. Fires Ask-AI-style highlight flash on sheet→bookmark transition (2s).
  - **Inline row** (`InstallPitch variant="inline"`) — amber strip in the You modal. Permanent escape hatch — visible whenever the 5-condition gate passes. Entire row is a clickable button that opens the sheet (`openSheetFromBookmark`). The platform-specific install button moved INTO the sheet to avoid nested-interactive HTML.

  **No timer-based suppression.** The prior 30-day `dismissedUntil` mechanism was removed because no surface fires it anymore — the sheet's X was replaced with chevron-minimise-to-bookmark (2026-06-02), and no other surface has a dismiss. Native install prompt cancel does NOT suppress — the OS cancel is the tail of an engagement, not a rejection of our pitch; the browser self-regulates `beforeinstallprompt` re-fire cadence and `deferredPromptRef` is consumed regardless. The `engaged` flag remains (set when `appinstalled` fires OR native prompt returns "accepted") — permanent gate for the lingering browser tab between accept and standalone-detection.

  **Sequential flow gating** (added 2026-06-02): the `protected` condition means the install pitch only fires for users who have completed the passphrase upgrade. Save-file → upgrade-to-passphrase → install. Plaintext-key users see the "Not protected" red banner instead, pushing them to upgrade first. Pinning an unprotected (plaintext WIF) key to the home screen is a security regression we don't enable.

  **Modal-overlap protection** (added 2026-06-02): `InstallContext` exposes a ref-counted `blockInstallPitch` / `unblockInstallPitch` pair. Rotation modals (MoveAddressModal / ChangePassphraseModal / RestoreModal) mount → block, unmount → unblock. Sheet fires only AFTER block clears. Prevents the install popup from sliding up over an active rotation modal. **Block-aware reveal (fixed 2026-06-26):** the 800ms reveal timer NOW re-checks the block at FIRE time, not just when armed — the unblock that arms it (e.g. ProtectModal unmounting) can race the You-modal's own close, so a modal / just-saved flow could still be on screen 800ms later (the owner saw the home-screen popup fire "at the same time" as the passphrase save). If still blocked at fire time the timer waits another beat and re-checks, so the sheet never slides up over the protect/passphrase/save flow.

  **Anti-patterns:** do NOT re-add a 30-day timer or any timer-based suppression. Do NOT couple `engaged` to anything other than completed install. Do NOT add an X to any surface — minimise to bookmark is the dismiss model. Do NOT replace the inline row's whole-row-click-opens-sheet with an inline install button (creates nested-interactive HTML and inconsistent platform behavior). Do NOT remove the modal-block ref-counter — without it, the sheet ambushes the user mid-rotation. See LAUNCH_PLAN.md Status #10, D4 (now superseded), `src/lib/install-pitch.ts`, `src/contexts/InstallContext.tsx`.

- **iOS ITP toast is sequenced AFTER welcome-gate completion (settled 2026-05-10, architect's collision fix).** On the first standalone-mode launch, when `isStandaloneMode() && !hasIdentity()`, the welcome gate fires FIRST. The iOS post-install storage-notice toast (`IosStorageToast`) fires only AFTER welcome-gate completion — never simultaneously. Implemented in `src/components/IosStorageToast.tsx`, gated by the `bsvibes_ios_storage_notice_shown` localStorage flag. Toast auto-dismisses after 8 seconds with a single "Got it" button. **Why sequencing matters:** if both conditions apply on the same launch (example: user installed → ITP eviction wiped data including the toast flag → user opens → no identity → both would fire), showing both at once is disorienting and the toast loses its contextual meaning. When sequenced, the toast becomes a post-gate *"by the way, this is what just happened"* confirmation — significantly better UX than a pre-emptive warning. Copy: *Headline "You're all set. One thing to know." Body "Apple may clear saved site data after long periods of inactivity. If that ever happens, your recovery file brings everything back in seconds — you're covered." Button "Got it"*. **Toast does NOT fire for Android users** (Apple ITP doesn't apply) or regular Safari tab users (they don't have the same eviction risk in the same form, and showing it would create false threat framing). **Do not move the toast trigger before welcome-gate resolution** — the sequencing is the fix for the welcome-gate/toast collision. See LAUNCH_PLAN.md Status #12, #12 note.

- **Standalone-mode detection uses three signals plus a reactive listener (settled 2026-05-10, architect's C4).** `useStandaloneMode()` (`src/hooks/useStandaloneMode.ts`) detects installed PWA mode via all three of: (1) `window.matchMedia("(display-mode: standalone)").matches` — the W3C standard signal, also fires for `minimal-ui` and `fullscreen` manifest display modes; (2) `navigator.standalone === true` — iOS Safari legacy property, required because iOS 16.3 and earlier do not fire the `display-mode` media query reliably in the initial render; (3) a 30-day `bsvibes_installed` localStorage flag set on `appinstalled` event capture (Android Chrome fires this event on successful install). All three must be checked; relying on only one or two produces false-negatives on either iOS or older Android. Additionally, the hook attaches a `matchMedia` change listener on the `display-mode` media query to reactively update when the user exits/enters Stage Manager or Split View on iPad. **Do not collapse to a single `navigator.standalone` check** — that is iOS-only and has the reliability caveat above. **Do not collapse to a single media query** — pre-iOS-16.4 installations are missed. All three signals must remain. The `appinstalled` flag is an additive belt-and-suspenders that prevents the install pitch from re-firing in the session immediately after install while the other signals catch up to DOM state. See LAUNCH_PLAN.md C4, D4.

- **Service worker integration with Next.js 16: four required guardrails (settled 2026-05-10, architect's C5).** When Bucket 3b lands, the service worker must be integrated following these constraints — all four are required; omitting any will produce either a broken Turbopack dev build, a broken iOS install flow, a CSP violation, or an insecure push subscription: (1) **No fetch listener.** See the "service worker scope discipline" decision above. In Turbopack dev mode, a fetch handler in the SW creates caching conflicts with Turbopack's own asset-serving pipeline and produces unreliable HMR. (2) **Register from a CLIENT component, not from `src/app/layout.tsx`.** `layout.tsx` is a Server Component. Service worker registration via `navigator.serviceWorker.register()` is a browser-only API and will throw during SSR if called from the server boundary. The correct registration site is a client component — specifically `IdentityProvider` (already client-side, lifecycle-managed, SSR-safe). (3) **CSP requires `worker-src 'self'`** in `next.config.ts`. Without this directive, the browser refuses to load the SW and the registration silently fails with a Content-Security-Policy violation. Verify in browser DevTools after any CSP change. (4) **VAPID public key ships as `NEXT_PUBLIC_VAPID_PUBLIC_KEY` env var.** Hardcoding the VAPID key in source creates a git-trackable secret rotation problem. The public key is not sensitive, but the private key is — keep the pair in Railway env vars, expose only the public key via `NEXT_PUBLIC_`. Generate with `webpush.generateVAPIDKeys()` during setup. **Any PR that adds a fetch handler, moves registration to layout.tsx, removes `worker-src 'self'`, or hardcodes VAPID keys is a regression against this decision.** See LAUNCH_PLAN.md C5, D6.

## Wallet Integration (future)

- Yours Wallet integration via `@1sat/connect` for power users
- Coexists with in-app wallet — not a replacement
- Not needed until later phases

## OpenCook Rebrand & Launch

> These decisions were settled 2026-06-13 during a launch-readiness review (three agent passes: irreversibility/extensibility, adversarial money/identity audit, and public-launch scope-gap). They define the critical path to OpenCook's first public launch.

> **STATUS (2026-08-01):** the rebrand described below is DONE (Phase 7, 2026-06-21) — the on-chain `app` tag is now `opencook`, the domain is `opencook.fun`, the repo is `github.com/Challotes/opencook`. So historical phrasing in these entries — e.g. "`app` stays `bsvibes`" (accurate only for the Phase-1 window *before* the Phase-7 sweep) and references to the deleted `migration.ts` (removed with key rotation 2026-06-14) — is HISTORICAL, not current. Kept as an honest record of the launch path.

- **Rebrand BSVibes → OpenCook before launch (settled 2026-06-13, SUPERSEDES the "Naming" decision at the top of this file).** The product launches as OpenCook. The rename is a name-only sweep across the entire naming surface — repo, all MDs, UI copy, the `bsvibes.com` domain, the `github.com/Challotes/bsvibes-` repo link, recovery-file name prefixes (`bsvibes-*`), localStorage key prefixes (`bfn_*`, `bsvibes_*`), the on-chain OP_RETURN `app` tag (`bsvibes` → `opencook`), the agent prompt, and OG metadata — performed LAST in the launch sequence so earlier fixes are not redone on renamed files. **Code/repo history, MDs, and session logs are KEPT** (renamed, not erased — they are the honest record of the build, legitimately attributed to the project). Positioning shifts "Agentic Fairness on BSV" → "on Bitcoin". **Execution hazard (verified against code):** the migration signed-message includes the `app` literal (`identity.ts:574-584`), the old key signs the exact message bytes, and the server re-encodes and verifies those exact bytes (`actions.ts:275-279`). So the `app` literal in the signer, the on-chain writers (`onchain.ts:25-33`, `migration.ts:59-67`, `boot-payment.ts:64-72`), and the verifier MUST be swept in ONE commit — a partial sweep breaks all key rotations (`invalid_signature`). Smoke-test a rotation immediately after the sweep.

- **Fresh user data from post #1 (settled 2026-06-13).** OpenCook's user data starts empty — the launch DB has no posts/boots/payouts/migrations, and the on-chain `app` tag flips to `opencook` so OpenCook's on-chain audit trail begins clean from record #1. This is the genuine contribution-tracking start point. Pre-launch BSVibes posts were test data; they remain on-chain under the old `bsvibes` tag, harmlessly ignored. **No localStorage migration** — pre-launch local identities are abandoned; users regenerate. This applies to USER DATA only; code/repo history is kept (see rebrand entry). Note: `post_id` inside on-chain boot-split records is the SQLite rowid (`posts.id` AUTOINCREMENT, `db.ts:20`; written to the payload at `boot-payment.ts:66`) and resets to 1 on the empty DB, so it is NOT a global on-chain identifier — readers must key on `(app, post_id)`.

- **On-chain records carry a top-level version field; discriminator harmonized (settled 2026-06-13).** All OP_RETURN payloads (post, migration, boot_split) gain a top-level integer `v` (starting `v: 1`) before the first real launch broadcast, and use ONE consistent kind-discriminator key (today posts/migrations use `type`, boot splits use `action` — `onchain.ts:30`, `migration.ts:61`, `boot-payment.ts:65` — harmonize onto one). **`v` is ADDITIVE — it does NOT replace the existing `formula_version` field** (`boot-payment.ts:70`), which versions the fairness math, not the record envelope. Rationale: on-chain records are permanent and currently carry no envelope version; without one, any future evolution of a record type makes all historical records ambiguous (can't distinguish "old record missing a field" from "new record with new meaning"). This is a feature-neutral extensibility guarantee — it names and builds no specific feature, it only preserves the option to add new record/contribution types later without breaking the existing audit trail or forcing a migration. **Must land in Phase 1, before any real broadcast — NOT bundled into the name-only rebrand**, or it risks being cut as "not name-only." **IMPLEMENTED 2026-06-14 (Phase 1 Steps 1 + 9):** posts/migrations got `v:1` + `type` in Step 1, but the PAID-boot OP_RETURN (`client-boot.ts`) was missed there — it was a positional field-array with no `v` and a `"boot"` discriminator. Step 9 closed that gap: a shared builder `src/lib/boot-audit.ts` `bootAuditPayload` now emits ONE JSON shape `{ v:1, app:"bsvibes", type:"boot_split", post_id, booter, funded, total, recipients?, formula_version?, ts }` for BOTH boot paths. `v` is the envelope version; `formula_version` (string) is retained separately as the decision requires. Added two NEUTRAL audit fields completing the boot record: `booter` (which address performed the boot — provenance; the server-funded path funds from the server wallet so the booter wasn't otherwise on-chain) and `funded` (`server`|`booter`, keeping the free-boot subsidy auditable). `app` stays `bsvibes` (the OpenCook rename is the separate Phase-7 name-only sweep). Code-auditor verified the larger payload doesn't break the txid/Step-7 ordering, the boot-confirm split check, or the fee/change-index accounting. **Step 9b (2026-06-14):** both audit-record writers (post in `onchain.ts`, boot_split in `boot-audit.ts`) now go through one shared envelope `src/lib/onchain-record.ts` `onchainRecord(type, body)` — so the `app` literal + `v` envelope live in ONE place (de-risks the Phase-7 rename's partial-sweep hazard) and the envelope can't drift between record types. The **READER CONTRACT is written down there** (a future consumer MUST: ignore unknown fields; select a stream by `(app, type)`; treat a missing `v` as legacy/v:0; bump `v` ONLY on a breaking field-meaning change, add optional fields freely otherwise). Adding a new on-chain parameter later is therefore a one-line, backward-safe change. **Timestamp caveat recorded:** `ts` is the writer's clock — server clock for server-built records (post, free boot) but the USER's browser clock for client-built records (paid boot); it is ADVISORY, the authoritative time is the block/confirmation time, and attribution logic must not trust a client-built record's `ts` as ground truth.

- **Identity keying is migration-chain-uniform, not raw-address (settled 2026-06-13).** Every money/attribution path must resolve a pubkey/address through the migration chain to its current head before granting, paying, or counting. The pre-launch economic bugs are all instances of this one seam applied inconsistently: free boots keyed by address so a rotation mints a fresh allotment (`pricing.ts:41`, `boot_grants` PK `db.ts:85`); earnings bootstrapped from the payouts table so a freshly-rotated address shows blank (`earnings/route.ts:27`); the creator bonus paid to the pre-rotation address with no chain resolution (`boot-orchestrator.ts:67`) — real funds to a dead address. Fixed as a CLASS in Phase 1, not bug-by-bug. `boot_grants` is re-keyed to the chain-root identity — **this is what makes the settled "15 per identity, never reset" decision actually TRUE across rotations** (today it's silently violated); the "free boots pay floor only" decision is also unchanged. **Implementation note:** `boot_grants.pubkey` misleadingly stores an *address*, while migration resolution is keyed on *pubkeys*, so the fix needs an address↔pubkey bridge (the payouts table links the two). **Chain-resolution failures fail toward PAID** (a DB error must NEVER fail-open into extra free boots).

- **Per-IP free-boot cap as additive defense (settled 2026-06-13).** A generous, sliding-window per-IP cap on server-paid free boots (keyed on the trusted `x-forwarded-for`) backstops the chain-resolved per-identity cap against the "endless private/incognito tabs each mint a fresh free-boot allotment" server-wallet drain (today the `bootPost` rate limit is keyed on the booter *address* — `actions.ts:214` — so rotating the address rotates the bucket). It STACKS WITH, and does NOT replace, the "15 per identity / floor price" rules (whichever binds first). Sized generously (well above 15) so it bites scripted abuse but not legitimate shared-NAT users (offices, campuses, carrier CGNAT). **Strength depends on the trusted-proxy `x-forwarded-for` config at deploy** — the header is client-spoofable without a trusted proxy (see CLAUDE.md "Deployment Notes" + memory `project_deploy_ip_header`), so the per-IP cap is a speed-bump; the chain-resolved per-identity cap is the primary control. (`bootPost` is a server action with no direct header access today — the IP must be threaded in the way the API routes already do.) **IMPLEMENTED 2026-06-14 (Phase 1 Step 6):** `src/lib/free-boot-cap.ts` `tryConsumeFreeBootForIp(ip)` — in-memory sliding window, **40 free boots / IP / rolling 24h** (reuses `rate-limit.ts`). `bootPost` reads the IP via `await headers()` (`next/headers`, first server-action header read in the repo) and consults the cap ONLY when the per-identity grant would make the boot free; if it binds, `isFree → false` and the boot routes to the existing paid path (silent free→paid flip, no error — `useBoot` already handles it). Recomputes the real `getBootPrice(db)` on demotion (the grant path returns price 0). **Fails toward PAID** on any error / missing-or-`unknown` IP — never fail-open into a server-funded boot. `boot-status` unchanged (cap surfaces only at boot time; doesn't leak the threshold). Note: with rotation removed the "rotating the address rotates the bucket" framing is moot — the attack is now strictly fresh-identity-per-tab farming, which this bounds. Code-auditor verified fail-closed + that paid boots can never be blocked.

- **Push notifications deferred to fast-follow (settled 2026-06-13).** OpenCook launches WITHOUT push notifications and WITHOUT a service worker. The core post → boot → earn loop does not depend on them, and deferring removes the service-worker + VAPID + permission-prompt surface from the launch critical path. They ship in the week after launch. The existing "Service worker scope discipline" + "Service worker integration with Next.js 16" decisions still govern when built.

- **Thin-core content moderation before launch (settled 2026-06-13, SUPERSEDES the LAUNCH_PLAN line scoping moderation entirely out of launch).** Because posts are written permanently on-chain, the only real control over bad content is a gate that runs BEFORE the post is logged on-chain — it cannot be retrofitted onto already-broadcast content. Verified: `createPost` validates + inserts then fires `logPostOnChain` fire-and-forget (`actions.ts`), and there is currently NO moderation/denylist/hidden-flag code anywhere in `src` (only a 1000-char length cap + signature check). Launch ships: (a) a pre-publication filter on the `createPost` path (denylist + length/rate) that runs before `logPostOnChain`; (b) a DB feed-level `hidden` flag + a minimal admin path to pull a post from the feed; (c) a visible "Report" affordance. Heavy moderation tooling (admin dashboard, report-handling workflow, automated image/hash scanning) is explicit FAST-FOLLOW. For a public, permanent-on-chain UGC product the write-side filter is launch-critical, not a separate post-launch stream.

  **REFINED 2026-06-16 (owner free-speech framing + right-sizing).** Narrowed to the genuinely thin core after a strategic discussion: **(1) the pre-publish filter is the one launch guard, scoped to the ILLEGAL FLOOR ONLY — NOT editorial/offensive content.** Rationale: the SERVER signs + broadcasts every post's OP_RETURN, so the operator is the *publisher* of permanent on-chain content — for the catastrophic-tail case (CSAM) that's criminal, not editorial. But legal-but-disagreeable speech STAYS — the product is censorship-resistant by design and editorial/opinion moderation is against the ethos + a free-speech slippery slope. Implemented: `src/lib/content-filter.ts` `screenContent()` called in `createPost` BEFORE the DB insert/broadcast; operator-supplied `CONTENT_DENYLIST` env var (NOT committed — no slur dump in a public repo; line/comma-separated substrings or `/regex/`); best-effort + EXTENSIBLE hook, NOT a comprehensive filter (a text denylist can't reliably catch coded/novel content — its value is good-faith effort + a hook to extend when a specific bad pattern is found); PERMISSIVE when unconfigured (over-blocking legal speech is against the ethos) — configuring it is a "before public launch" operator gate. Unit-tested (`content-filter.test.ts`). **(2) The hidden-flag / admin-pull / Report system is DEFERRED — handle the rare display case manually.** Building a moderation apparatus contradicts the censorship-resistant ethos; the operator can pull a DB row by hand if ever needed. **(3) Architecture options CONSIDERED + REJECTED this session (recorded so they're not re-litigated):** (a) *client-broadcast posts / faucet onboarding* (user signs+broadcasts their own post → operator becomes a true neutral conduit) — REJECTED: requires users to hold sats, which breaks the free zero-friction 2-click posting that is the product's core magic (even a faucet doesn't save it + adds a drain surface); (b) *posts off-chain, only boosts on-chain* — REJECTED: provable, timestamped, on-chain attribution of every post IS the product (the automated fairness / provable-first-credit engine depends on it); (c) *obfuscating/encoding the OP_RETURN* — REJECTED: encoding ≠ not-publishing (substance over format; you still broadcast it; deliberate obfuscation can read as concealment = legally worse), AND you WANT the record readable + verifiable for provable attribution. **(4) Legal = plain disclaimers** (ToS / Privacy / permanence acknowledgement) as lawyer-ready templates with `[TODO]`/`[LAWYER]` markers; operator legal name/jurisdiction stay `[TODO]` (Hard Rule #6 — never committed). **THREE risks genuinely need ~1hr of a real lawyer BEFORE public launch** (a template can't cover them): GDPR/CCPA erasure-vs-immutable-chain wording; CSAM/illegal-content exposure when the operator is the broadcaster (the pre-publish filter is the load-bearing mitigation — must be configured + live); money-transmitter characterization of the no-custody + free-boot-subsidy + 5%-cut model. These are a "before public launch" OWNER action, NOT a blocker to building. **DO NOT** build editorial/opinion moderation, take posts off-chain, or obfuscate the OP_RETURN.

- **Thin-core legal before launch (settled 2026-06-13).** Launch ships a plain-language Terms of Service (user responsibility for posted content + a prohibited-content clause that backs the moderation policy), a Privacy Policy, and an explicit permanence disclosure: posts are permanent and public on-chain; identity and earnings are lost if the recovery file is lost; there is no account recovery; do not post personal data because the chain cannot be erased. A full legal review at scale is FAST-FOLLOW, not a launch blocker. The no-custody payment model (the transaction IS the payment; the server never holds user funds — paid boots are client-built in `client-boot.ts`) is the load-bearing defensive posture and is stated plainly. (Not legal advice — a real review is owed before scale.)

- **Key rotation REMOVED in favor of encrypt-in-place (settled 2026-06-13). SUPERSEDES: the "5-minute window problem" rotation premise, "Key rotation on upgrade", "Every key rotation produces an encrypted key" (2026-04-17), "Pre-rotation chain verification" (2026-04-17), E29 "Restore of rotated keys is blocked outright", E30 "stale-key session-lockout", E31 "block rotate-from-stale", and the "Identity keying is migration-chain-uniform" entry above (the bug class it described is deleted, not fixed).** Validated by three independent agent reviews (irreversibility/extensibility, threat-model, removal-feasibility) on 2026-06-13.

  **New model:** the auto-generated key/address NEVER changes for the life of an identity. No rotation, no on-chain migration, no migration chain, no sweep, no stale-key state. Adding a passphrase ENCRYPTS THE EXISTING key in place (re-encrypt the same WIF under the passphrase → write `bfn_keypair_enc` → remove plaintext). Changing a passphrase decrypts-with-old / re-encrypts-the-same-key-with-new. **Recovery-file export is gated behind having a passphrase** — "Save your account" prompts for a passphrase first, so NO unencrypted recovery file can ever be created or downloaded. This is the load-bearing mitigation: it closes the only realistic plaintext-leak vector (an unencrypted file the user downloads then syncs to the cloud). Restore = import a key from a file (decrypt with passphrase); no eligibility/migration check.

  **Why:** rotation was a correct implementation of an expensive premise ("a plaintext key must be revocable"), but it spawned the entire address-vs-migration-chain bug class (free-boot reset on rotation, blank earnings for a rotated address, creator-bonus paid to a dead address) plus ~2.5–3.5k LOC and the E29/E30/E31 subsystems. Encrypt-in-place is a cheaper premise: keep the plaintext window short, never export an unencrypted file, and accept that a key stolen BEFORE it was ever protected is simply lost. Removing rotation DELETES the bug class rather than fixing it, and a stable key/address makes "15 free boots never reset" and "earnings follow the user" true for free. Pre-launch (fresh-from-post-#1, empty DB) is the only cheap moment — post-launch, once real on-chain migration records exist, removal becomes permanent legacy support.

  **SECURITY REGRESSION ACKNOWLEDGED (Hard Rule #3) — owner signed off 2026-06-13.** This removes in-app REVOCATION of a key that leaked while plaintext (the window between first visit and setting a passphrase — via malware reading browser storage off disk, a device backup, or a shared device). Encrypt-in-place cannot un-leak a copy already taken. Note: E29/E31 (marked FIXED in SECURITY_AUDIT) defended attack surfaces that ROTATION itself created (rotate/restore-from-stale takeover) — those vectors cease to exist once rotation is gone, so they are *obsoleted*, not regressed. The genuinely-lost property is revocation. Accepted at launch scale because: (a) the passphrase-gated-export rule closes the most realistic vector; (b) under the old model the revocation benefit only ever accrued to users who upgraded — most never did and already held an unrevocable plaintext key; (c) re-adding revocation later is cheap and additive. SECURITY_AUDIT.md E29/E30/E31 entries get updated to "superseded by encrypt-in-place" when the code removal lands (Phase 1).

  **Orphaned-data scar is MOOT for OpenCook:** deleting the chain resolver would normally sever a rotated user's old-address earnings — but the fresh-from-post-#1 launch DB has no rotated users, so there is nothing to orphan.

  **Future re-add path (recoverable):** the removal commit will be git-tagged. A future "I think my key was exposed" capability would be an OPT-IN, rare reclaim action — NOT rotation-on-every-upgrade — reusing the git-recoverable migration OP_RETURN format + signature-verification + chain-resolution pieces, not the old rotation-on-protect shape. Keep the on-chain migration payload format documented so a re-add is a design conversation, not archaeology (precedent: the `cleanupMigrations` removal note above).

  **DO NOT** re-introduce rotation-on-passphrase-add, the migration chain resolver, or the E29/E30/E31 machinery without explicitly superseding this decision. **DO** keep the passphrase-gated export rule — no unencrypted recovery file ever leaves the device.

- **Money surfaces key on the BSV address (settled 2026-06-13).** With rotation removed, key == address == pubkey (1:1, stable for life), so every money/attribution lookup, grant, aggregation, and rate-limit bucket standardizes on the BSV ADDRESS as the single canonical identifier. `pubkey` is retained ONLY as a stored attribute for ECDSA signature verification (`createPost`) and as an audit artifact — it is not a lookup key. Rationale: payout destinations are already addresses (P2PKH), `boot_grants` is already keyed by address, and `/api/earnings` already queries by address — so this is the lowest-churn unification and needs zero data migration on the fresh launch DB. Resolves BUG-6 (boot-confirm currently stores a pubkey in `bootboard.boosted_by` while the free path stores an address, so paid boots never appear in a user's earnings "spent" feed) by deriving the address from the booter pubkey on the confirm path. Implemented in Phase 1.

- **Free-boot path consumes the grant BEFORE paying (settled 2026-06-13, NUANCES C5).** For SERVER-FUNDED free boots, the grant (`free_boots_used`) is consumed before the broadcast, with an idempotency key on the txid/nonce, so a crash between a successful broadcast and the DB write cannot let the user retry and make the server pay twice. This is the opposite bias from C5 (SECURITY_AUDIT, "free boot consumes grant only after successful broadcast") — deliberately, because the two protect different parties: C5 protects the USER's free boot when their own broadcast fails (client-funded reasoning), whereas the free-boot path is spending the SERVER's money, so the server wallet must be protected against repeatable double-pay. Worst case under the new bias = "user loses one free boot but the server pays once" (matches the C5 risk framing) instead of "server pays repeatedly." The paid (client-funded) path keeps its existing semantics. Implemented in Phase 1 (per-IP cap + idempotency). **IMPLEMENTED 2026-06-14 (Phase 1 Step 8):** `executeBoot` (`boot-orchestrator.ts`) now consumes `free_boots_used` in an atomic SELECT-then-conditional-UPDATE `db.transaction` BEFORE calling `buildSplitTransaction`. The monotonic counter IS the idempotency key (there is no client txid for a server-built free boot — the "txid/nonce" framing in the original decision is satisfied by the counter). The atomic transaction also closes the concurrent-double-click TOCTOU; a grant exhausted concurrently returns `FREE_GRANT_EXHAUSTED` and `bootPost` routes it to PAID (no error flash). No refund on broadcast failure (ambiguous-failure safety). C5 status updated to REVERSED. Code-auditor verified the double-pay window is closed + no double-increment / PK collision.

- **Attribution is timestamp-arbitrated and self-correcting; just don't deliberately pre-claim (settled 2026-06-13).** OpenCook's thesis is provable first-credit: whoever contributes an idea first — in the repo OR on-chain — is attributed, and the immutable timestamp (commit time vs on-chain post time) is the objective arbiter. Once live this resolves itself — the record decides automatically, which is the fairness mechanism itself. So it needs no perfect exactness now: the only pre-launch discipline is to not deliberately pre-document specific unbuilt ideas we want the community to have a genuine first chance at. Existing vision text already in the repo (DIRECTION.md's recursive/spawning model, FUTURE.md, ROADMAP Phase 7) is **KEPT** — it was there first, legitimately attributed to the project; erasing it would be dishonest in the other direction. The rule is forward-only: don't ADD named unbuilt features to commits; build with neutral extensibility headroom (see the on-chain version-field decision) so community-surfaced ideas slot in later; don't over-engineer beyond that — the ledger settles who was first.

- **Rotation removal IMPLEMENTED (2026-06-14).** The 2026-06-13 "Key rotation REMOVED in favor of encrypt-in-place" decision above is now landed in code across five commits: `7fc68ff` (E30 stale-key client machine removed), `10633fa` (version-gated restore policy + E29 preflight + server-side E30 emitter removed), `5719988` (rotation backend — MoveAddressModal, migration.ts, restore-eligibility route, sweep/upgrade in identity.ts, BackupData rotation fields), `062dcd0` (migrations table + chain-resolution code removed from db.ts/weights.ts/earnings). Each group verified tsc + Biome + tests + code-auditor clean. `ProtectModal` (encryptInPlace) and `ChangePassphraseModal` (changePassphrase) were already in place from the earlier encrypt-in-place work; passphrase protect + change (same key, unlimited times) is verified intact. **Restore policy shipped as a version stamp:** `backup-template.ts` stamps `fileVersion: 1` (`RECOVERY_FILE_VERSION`) into every generated file; `restore-from-file.ts` rejects any file that is plaintext OR lacks `fileVersion === 1` as `unsupported_version`. So ALL legacy files (plaintext WIF and pre-stamp encrypted) are rejected — the owner's "start clean" decision. `importIdentity` (plaintext import) deleted; restore is encrypted-only. **Migration/payout history confirmed disposable test data** (fresh OpenCook start), so the migrations table + chain resolution were removed, not just left dormant; the simplification is behaviorally identical for every never-rotated user (pubkey→address is 1:1, so earnings attribution is unchanged). **FAIRNESS.md's AFP novelty claim** ("contribution history following key rotations via OP_RETURN chain resolution") is kept as a HISTORICAL prior-art note (built + logged on-chain, recoverable in git, removed at launch for encrypt-in-place) per owner decision 2026-06-14 — not deleted. Remaining doc-sync (scrub the stale "live behavior" descriptions in CLAUDE.md, ROADMAP.md, FAIRNESS.md, SECURITY_AUDIT.md E29/E30/E31 entries → "superseded by encrypt-in-place") is the last step before the rest of Phase 1's server items (on-chain `v:1` field, per-IP free-boot cap, boot-confirm auth, free-boot idempotency).

- **Paid-boot confirm records from on-chain outputs, never recompute-and-reject (settled 2026-06-15, deep-audit Finding 6).** `/api/boot-confirm` used to RE-derive the contribution split (price × weights) and reject (HTTP 400) if the already-broadcast tx's outputs didn't match. But weights/price legitimately drift between the boot-shares quote and the confirm POST (every other boot mutates `bootboard` → weights; a new post moves the price), so this rejected honest, already-on-chain txs → the client retried → built a NEW tx (new txid) → DOUBLE-PAID (the replay guard is txid-only). New model: the tx is self-authenticating (`hash(rawTx)===txid`) and already broadcast, so boot-confirm RECORDS the payouts FROM the parsed on-chain outputs (`txVouts`) — it does not re-adjudicate the split. **One enforced conservation invariant:** the platform must receive ≥ `floor(bootPriceFloor × platformCut) − 2` sats on-chain (uses the FLOOR, not the drift-prone recomputed price, so it never false-rejects; the platform address is the server wallet, never the booter). **Client (`useBoot`):** after a successful broadcast it NEVER rebuilds a new tx on confirm failure — it re-POSTs the SAME confirm body (idempotent: a duplicate hits the 409 txid replay guard) on transient 503/network-throw, and only rebuilds on TX_CONFLICT (the tx never landed). **Accepted residual (Hard Rule #3 — net STRONGER than before, which had no total floor at all):** a client can under-pay the *non-platform* contributors and still get a boot recorded + bootboard credit — but that's their own money, contributors are credited exactly what was sent on-chain (honest ledger), and a per-contributor floor is impossible because the booter can themselves be a pool recipient (it would false-reject a solo contributor). `recipient_pubkey` is now just an audit label (earnings joins on `recipient_address` — see "Money surfaces key on the BSV address"). The booter's own change output (and, as a minor consequence, a booter-contributor's own pool share) is excluded from recorded payouts. Code-auditor verified the double-pay vector is closed (incl. the thrown-fetch path) and no forged/phantom payout is possible. The FREE/server-funded path (`boot-orchestrator`) is unchanged — it correctly trusts its own server-built split because the server IS the payer there.

- **Balance shows spendable (confirmed); deposit shortfall includes the network fee (settled 2026-06-15, device-test findings).** On-device QA showed the balance pill counting unconfirmed (0-conf) UTXOs as spendable — displaying ~7,687 when only 5,023 was confirmed/spendable — and a paid boot failing "not enough funds" because the displayed cost omitted the network fee (5,023 − 4,836 = 187 < fee). Both are display/affordability-honesty bugs, NOT money-loss (every sat was always accounted on-chain). Decisions: (1) `/api/balance` returns `balance` = CONFIRMED (spendable) only, plus a separate `pending` (0-conf, by WoC `height`). The UI headline is spendable; pending shows as a muted "+X pending" line. Rationale: the displayed number must answer "what can I spend right now" — a pending-inclusive total made boots look affordable when they weren't, and made the balance look like it didn't drop after a boot (pending change re-inflated it). (2) The deposit/top-up shortfall is measured against price + network fee, not price alone. `clientSideBoot` surfaces the fee it already computes — `estimateFee(1, outputCount)`, the exact threshold the `insufficient_funds` branch tests against — through `useBoot` → `Feed` → `FundAddress`. Because that branch is only reached when `balance < price + fee` (the `needs_consolidation` branch creams off the affordable-after-consolidation case first), the shortfall is provably always strictly positive, so the modal can never again say "you have enough" after a real failure. Auditor-verified PASS: pure value-surfacing, NO change to coin selection / fee estimation / signing / broadcast / spent-set / 0-conf chaining. (3) **SEMANTICS NOTE — do not conflate two `balance` numbers:** `FundAddress.balance` (and the displayed pill) now means CONFIRMED/spendable, while `clientSideBoot`'s internal `balance` (~line 381) still sums ALL fetched UTXOs because it legitimately spends 0-conf change via 0-conf chaining. They mean different things BY DESIGN. No security control affected. **DO NOT** revert `/api/balance` to a confirmed+unconfirmed sum, or drop the fee from the affordability/shortfall math.

- **Broadcast timeout is INDETERMINATE — never rebuild (settled 2026-06-16, Phase 2 Build A).** The server wallet (`wallet.ts`, funds free boots + post-logging) holds a mutex across 4 external calls that had NO timeouts — any hang froze ALL free boots + post-logging site-wide until the socket died. Build A added timeouts: **10s on the read calls** (unspent / source-tx-hex / double-spend lookup — abort + fail-safe, no money moved, safe to retry) and **30s on `tx.broadcast()`**. The load-bearing rule: a broadcast timeout does NOT mean the tx failed — ARC may have accepted it slowly. So it returns a distinct terminal `broadcast_timeout` status that **NEVER triggers a rebuild** (a rebuilt tx = a new txid = the server DOUBLE-PAYS). On timeout the wallet **releases the UTXO reservation but does NOT blacklist the inputs** (WhatsOnChain reflects on-chain reality; the change output resurfaces via WoC if the tx landed; rare stale-WoC reuse is caught by the existing `DOUBLE_SPEND_ATTEMPTED` self-heal) and does NOT register change. Both retry sites skip it: `boot-payment.ts`'s 1s auto-retry and `onchain.ts`'s post-log retry only re-run the did-not-broadcast failures. The free grant was already consumed pre-broadcast and is NOT refunded (consistent with "Free-boot path consumes the grant BEFORE paying"); the orchestrator surfaces `indeterminate:true` → `bootPost` returns it with **NO `error`** → `useBoot` releases quietly with **NO "tap to retry"** (a manual retry would rebuild a fresh tx and double-pay the same post). Auditor-verified PASS: the mutex always releases (outer `finally`), no `broadcast_timeout` path can reach a rebuild, `withTimeout` clears its timer + a late-resolving broadcast can't mutate state, no security control weakened. **DO NOT** make a broadcast timeout retry/rebuild, and **DO NOT** blacklist-on-timeout (a false timeout would strand the wallet's main UTXO until restart). Read-call timeouts ARE safe to retry. Builds B (balance precheck → route free→paid + low-balance alert), C (env-var kill-switch, fail-closed, pre-consume), D (broadcast proxy) follow.

- **Dry server wallet routes free boots to paid (settled 2026-06-16, Phase 2 Build B).** Previously a dry server wallet consumed the user's free-boot grant THEN failed at broadcast with no refund — burning the grant AND erroring (the device-test rough edge). Build B adds a balance precheck in `executeBoot` BEFORE the grant consume: read `getBalance()` (spendable); if it can't cover `actualPrice + SERVER_FEE_BUFFER_SATS`, route the boot to PAID (`isFree:false`, real dynamic `getBootPrice(db)` → `bootPost` returns `requiresPayment`), exactly like the `FREE_GRANT_EXHAUSTED` path. The fee figure is EXACT: a split's outputs total === `actualPrice` (no rounding drift — the `Math.floor` remainder folds into the creator output), and `SERVER_FEE_BUFFER_SATS` (500) is now exported from `wallet.ts` and used by BOTH the precheck and `reserveUtxos`'s `totalNeeded` — so the precheck can NEVER green-light a boot that then hits `insufficient_funds` and burns the grant. **Fails toward PAID:** a WoC read failure makes `getBalance` read low/0 → paid, never fail-open into a doomed broadcast (the precheck cannot throw — `getUtxos` swallows all errors to the `_pendingChange` fallback). The precheck sits BEFORE the consume, so it does NOT relitigate "no refund on broadcast failure" — it avoids REACHING the consume when the server can't pay. Plus a debounced (5-min) low-balance console alert at `serverLowBalanceAlertSats` (10k sats, `config.ts`) — alert-only, never blocks, fires on the free-boot path (where the server drain is). Auditor PASS (no grant-burn path, no fail-open, fee-buffer sync exact). Accepted residuals: one extra WoC fetch per free boot (Build D cache-reuse), a NARROW precheck→reserve TOCTOU that can still burn one grant under concurrent UTXO contention (Build B reduces, doesn't eliminate, the burn window — consistent with the settled no-refund policy). Real alerting (webhook/email) is Phase 5. **DO NOT** move the precheck after the grant consume, and keep the precheck threshold in sync with `SERVER_FEE_BUFFER_SATS`.

- **Server-wallet kill-switch — env var, fail-closed, pre-consume (settled 2026-06-16, Phase 2 Build C).** `isServerSpendDisabled()` (`wallet.ts`) reads `BSV_WALLET_SPEND_DISABLED` (truthy `"true"`/`"1"` = tripped). When tripped the server wallet STOPS spending: (1) `executeBoot` checks it at the TOP of the `if (isFree)` block — BEFORE the grant consume — and routes free boots to PAID (`isFree:false`, `error:"SERVER_SPEND_DISABLED"` → `bootPost` → `requiresPayment`), so tripping it consumes NO grant; (2) `buildAndBroadcast` returns a new terminal `{status:"spend_disabled"}` as a backstop that also stops post-logging + any direct server spend (skipped in both retry sites). **Paid/client boots are UNAFFECTED** — they spend the user's own funds via `client-boot.ts`, and boot-confirm's server re-broadcast uses `parsed.broadcast()` (NOT the gated `buildAndBroadcast`), so killing free boots can't break paid boots (auditor-confirmed the one over-reach risk). Env var (v1) → takes effect on redeploy; a DB-backed instant runtime toggle is the documented fast-follow (the auditor's "fail-closed on unreadable" applies to that DB read, not to an env read which can't throw — `process.env.X ?? ""`). Default unset = spending enabled (correct for an env var — you don't want spending dark by default). Only exact `true`/`1` trip it (a typo reads as enabled = the safe direction). Unit-tested (tripped → no grant consumed, no broadcast, routes to paid). Auditor PASS (a–f). **DO NOT** gate the paid/client path or move the switch check after the grant consume.

- **Durable post-retry: a post broadcast TIMEOUT re-sweeps — distinct from the boot no-rebuild rule (settled 2026-06-19, Phase 4 Wave 1).** Locked invariant: *every accepted post must land on-chain — no off-chain orphans.* Before this, a post whose OP_RETURN broadcast timed out (or failed after the inline 1s retry) returned `null` and was **orphaned forever** (`tx_id` stayed NULL, nothing re-attempted it). Wave 1 makes `tx_id IS NULL` a durable retry queue drained by an ambient-traffic single-flight sweep (`src/services/bsv/anchor-sweep.ts` `sweepOrphans()`, fired fire-and-forget from `createPost` and `GET /api/posts`; in-memory exponential backoff; one broadcast per sweep; 90s min-age so it never races the inline first attempt). **This intentionally DIVERGES, for POSTS ONLY, from "Broadcast timeout is INDETERMINATE — never rebuild" (the Phase 2 Build A entry above):** that rule exists because a rebuilt BOOT tx mints a new txid and the server **double-pays contributors** — a *financial* hazard. A post-log has **NO payee** (a 0-sat OP_RETURN), so re-broadcasting a timed-out post costs — in the rare case the original actually landed — **one wasted ~66-sat OP_RETURN** (~$0.0000077, a rounding error), not a double-pay. NOT retrying would instead permanently orphan a never-landed timeout, violating the invariant. So **boots keep the strict no-rebuild rule; posts re-sweep until anchored** — the two policies coexist by design, keyed on the no-payee property, NOT a drift. A rare duplicate on-chain record is harmless (the `onchain-record` reader contract keys on `(app,type)`; the fairness engine attributes from the one DB row per post, never from counting OP_RETURNs). Zero schema change (the queue is `tx_id IS NULL`); in-memory backoff lost-on-restart = an immediate retry after deploy, which is desirable. Kill-switch / dry-wallet stragglers stay NULL and drain gradually (one-at-a-time on ambient traffic, not a flood) once spending resumes. **DO NOT** extend re-broadcast-on-timeout to the boot path (`boot-payment.ts` / `boot-orchestrator.ts`) — that reopens the double-pay vector Phase 2 Build A closed.

- **Phase 4 abuse/cost caps — all-posts-on-chain + refuse-gates + spend ceiling + boot-price anti-inflation (settled + built 2026-06-19, Waves 0–3).** Locked principle: **every ACCEPTED post must land on-chain — no off-chain orphans.** A post the server can't/won't fund on-chain is REFUSED upfront, never stored. Built lean — the heavy options (a setInterval retry worker, a DB-backed budget) were deliberately rejected as over-engineering per the 2026-06-13 "neutral extensibility headroom" decision.
  - **Wave 0 — rate-limiter correctness fix** (`rate-limit.ts`): cleanup pruned every key against the FIRST caller's window; a 60s-window caller registering first pruned the 24h free-boot-cap's timestamps after ~60s, silently defeating that cap (server-wallet drain). Now each entry stores its own window. A real latent bug in a SECURITY_AUDIT-FIXED control.
  - **Wave 1 — durable on-chain retry** (`anchor-sweep.ts`): see "Durable post-retry: timeout => re-sweep" above. Money-path auditor CLEAN.
  - **Wave 2 — refuse-gates + daily spend ceiling.** `createPost` runs three gates BEFORE the insert (so a refused post is never stored off-chain): (1) per-IP **200/day** on-chain-post cap (env `ONCHAIN_POST_IP_LIMIT`, reuses `rateLimit`) → refuse `daily_limit`; (2) `isServerSpendDisabled()` → refuse `paused` (kill-switch now REFUSES new posts rather than insert-then-strand — closes a prior orphan source); (3) `hasDailyBudget()` → refuse `paused`. The **in-memory daily spend ceiling** (`server-spend-budget.ts`, env `SERVER_DAILY_SPEND_SATS`, ~$0.20/day default) caps total server-wallet spend across BOTH post-logging AND free boosts (shared wallet) — the aggregate backstop behind the per-IP caps. `bootPost` checks the ceiling BEFORE consuming the grant/IP-cap → routes free→paid when blown (boost still happens, user funds it; no grant burned). BLOCK-at-200 protects all three surfaces (wallet, boot-price, DB), vs the rejected skip-logging which protected only the wallet. **In-memory by design:** a redeploy could let one UTC day exceed the ceiling once (~2x ~$0.20 = ~$0.40, trivial vs the ~$50/mo budget). **Accepted residual (OBS-2):** `hasDailyBudget`/`recordDailySpend` aren't atomic — N concurrent requests at the boundary over-admit by N×cost (cents); bounded, consistent with the ~2x tolerance, not worth refund complexity. **Indeterminate free-boost broadcasts ARE recorded** (OBS-1 fix — presume-spent, the safe direction for a ceiling).
  - **Wave 3 — boot-price anti-inflation** (`pricing.ts`): the dynamic price counted `COUNT(DISTINCT pubkey)`, so a distributed attacker minting one fresh identity per post could pin the price at the 250k ceiling, griefing real payers. Now only ESTABLISHED contributors count — pubkeys with ≥ `minPostsForPricing` (3) posts in the window (`HAVING COUNT(*) >= 3`). **Pricing query ONLY** — the payout split (`weights.ts`/`split.ts`) does NOT need the gate (a 1-post identity earns a vanishing `sqrt(1)×decay` share, not free money); gating payouts would be a separate economic-policy choice, deliberately NOT bundled.
  - **DEFERRED (no invariant weakened):** rate-limiter LRU key-cap (store self-cleans), persisting the agent daily counter (in-memory, matches existing design), prompt `cache_control` (needs a stable-prefix refactor first; post-launch cost pass). The user-funded posting escape valve stays deferred — at a 200/day block it only bites abuse, but it's the eventual companion so a blocked user always has a paid route on-chain.
  - **Forward-compat (future idea-attribution):** a rare duplicate on-chain post record (Wave 1 timeout re-sweep) is SAFE for any future "pay the idea for its contribution" mechanism — it must key on EARLIEST on-chain timestamp + dedupe by idea (content/signature), NOT naively sum on-chain occurrences. Same author (pubkey/sig), later timestamp → the original is canonical, the duplicate ignored. ("First to say it on-chain" wins.)
  - **Deploy (OBS-3/OBS-4):** per-IP caps require a trusted proxy setting `x-forwarded-for`/`x-real-ip`; without one, header-less requests share a `postIp:unknown` bucket (fails toward REFUSED — an availability cliff, not a money hole). New env vars `SERVER_DAILY_SPEND_SATS` + `ONCHAIN_POST_IP_LIMIT` documented in `.env.example`. **DO NOT** make the refuse-gates run after the insert (would store off-chain), and **DO NOT** add the ≥3 gate to the payout split without an explicit economic-policy decision.

## Threading — `parent_id` + denormalised `root_id` (built 2026-08-14, server side; UI not built)

Full spec and rationale live in THREADS.md; this records the decisions that are settled and
the two places the build deviated from the spec.

- **Threading exists to serve the token model, not to add a comment feature.** TOKENS.md's
  shape is: a thread is marked with a ticker and minted, and a parent token takes a stake in
  each child. That requires a thread to be a *thing* with an identity. `posts` was flat, so
  there was nothing for a ticker to attach to. Threading is the prerequisite, which is why
  it was built before any of the token layer.

- **Store `root_id` as well as `parent_id`, denormalised.** `parent_id` alone gives the tree
  but makes "every post in this thread" a `WITH RECURSIVE` walk — a query that would sit on
  the token-allocation path and run per mint and per payout. Denormalising the thread root
  onto every row makes it one indexed lookup. A root carries `root_id = id` (self-rooted),
  set by an `UPDATE` immediately after insert **inside the same transaction**, because a
  root's id cannot be known before the row exists. The cost is one column and one write-time
  rule; the existing 2,006 posts were backfilled in `db.ts` so no query ever needs the
  `root_id IS NULL OR id = ?` compatibility form.

- **Two indexes, not three — corrected by measurement.** The spec called for a partial
  `idx_posts_roots ON posts(id DESC) WHERE parent_id IS NULL` to serve the root feed.
  `EXPLAIN QUERY PLAN` over 50 roots + 2,000 replies shows SQLite never chooses it: `id` is
  `INTEGER PRIMARY KEY` (the rowid), so an index on `(parent_id)` physically stores
  `(parent_id, rowid)` and walking its `parent_id IS NULL` span backwards already yields
  `ORDER BY id DESC` with no sort step. The partial index would add write cost on every
  insert and buy nothing. Dropped, with a test asserting it stays dropped.

- **The parent is looked up, never trusted.** `parent_id` arrives from the client and is
  **not** covered by the post signature (which signs content only). A parent id pointing at
  nothing would create a post that can never be rendered — excluded from the feed for having
  a parent, and absent from every thread view for pointing at a root that isn't there. So
  `createPost` resolves the parent from the DB and returns `invalid_parent` if it does not
  exist. **DO NOT** take `root_id` from the client; derive it from the looked-up parent.

- **The feed is roots-only, via one shared `ROOTS_ONLY` constant.** Every feed read filters
  `p.parent_id IS NULL`. Missing it on a single query leaks replies into the feed out of
  context and duplicated under their own thread — worst on `getNewPosts`, polled every 5s,
  where a reply would pop into every open feed live. Cursor pagination is unaffected:
  `Feed.tsx` pages on `id < before` / `id > after`, and filtering rows out cannot break a
  cursor while the column stays monotonic and unique. What *does* change is that id gaps
  grow — **do not introduce code that infers "how many posts exist" from an id delta.**

- **Arbitrary reply depth, rendered flat.** Capping at depth 1 was the simpler option and
  sufficient for the token model, but it destroys information irreversibly. Arbitrary depth
  costs one extra column read given `root_id`, and preserves the option of a "replying to"
  chip later.

- **Boot counts stay PER-POST for now.** Thread-aggregated boot counts (a root's count
  summing every boot in its thread) is probably what the token model eventually wants, but it
  changes `weights.ts` engagement scoring and the `bootboard` spotlight semantics
  simultaneously. Shipped per-post so that a feed regression and a payout regression cannot
  arrive in the same commit. Thread aggregation is a separate change with its own tests.

- **`weights.ts` deliberately unchanged.** Replies are posts with a `pubkey`, so they earn
  contribution weight automatically under the existing query. That is the correct default —
  a reply is a contribution — and it is recorded here so nobody "fixes" it. No file under
  `src/services/fairness/` changes for threading, and `bootboard` / `boot_grants` /
  `payouts` all still key on `posts.id`, which still means the same thing.

- **On-chain: `id` travels with `parent` (deviation from the spec).** THREADS.md specified
  adding `parent` alone to the post OP_RETURN. But a post record carried no identifier of its
  own, so `parent: 41` would have named a row no chain reader could locate — leaving the
  thread graph reconstructible only from SQLite, which is the one thing that step exists to
  fix. The record now carries the post's own rowid too, matching the convention `boot_split`
  already uses (`post_id`, keyed as `(app, post_id)`). `parent` is written as an explicit
  `null` for roots rather than omitted, so a root written by a threading-aware writer is
  distinguishable from a pre-threading record. Both are additive optional fields, so **`v`
  stays 1** per the reader contract in `lib/onchain-record.ts` — bumping it would orphan
  readers of the 2,006 already-anchored genesis records.

- **`anchor-sweep.ts` must forward both fields.** The sweep is the ONLY path that anchors a
  post whose inline broadcast failed, and an OP_RETURN is immutable — a reply swept without
  its parent is unthreadable from the chain forever, with no second attempt. The record shape
  is pinned by `onchain.test.ts` and the sweep's forwarding by
  `anchor-sweep-roundtrip.integration.test.ts`, because this is the one part of the codebase
  where a mistake cannot be corrected by a later deploy.

- **The thread view is an OVERLAY, not a third feed mode (step 4, 2026-08-14).** The feed's
  LIVE/ORIGIN machinery — bottom-relative prepend anchoring, the landing effect, the two
  IntersectionObserver sentinels, the unread watermark — each assume the scroll container
  holds the root feed, and each was hard-won (see "Feed: LIVE/ORIGIN modes"). A third mode
  would have put all of them in play for a feature that needs none of them. `ThreadView`
  mounts OVER the feed with its own scroll container: the feed keeps polling underneath, its
  scroll position is never touched, and closing restores nothing because nothing moved.
  **DO NOT** convert this into a feed mode or a route — a route would remount the feed and
  lose its scroll position, which is the same failure by a different path.

- **The reply composer is `PostForm` with a `parentId`, not a second composer.** A reply is a
  post, so it must get the same signing, the same one-time permanence gate, the same
  `requireIdentity()` sign-in gate and the same on-chain anchoring. A separate composer would
  have been two places to keep all four in step. `compact` drops the footer chrome (install
  bookmark, Ask AI, keyboard hint) that belongs only to the feed's primary box.

- **Replies target the ROOT, not the tapped post.** Arbitrary depth is stored and queried,
  but the render is flat (settled above), and a per-reply reply button would silently
  produce nesting the UI cannot show. Depth stays available for a future "replying to" chip.

- **`reply_count` rides the feed query AND the live-count channel (both needed).** It is
  computed by the `POST_SELECT` join like `boot_count` — one query, no second round-trip, and
  it cannot drift from the row it is rendered beside. That covers first load. It does NOT
  cover updates: **nothing re-fetches a root row when a reply lands**, because `getNewPosts`
  filters replies out (they are not roots) and `getUpdatedPosts` only returns posts that
  gained a `tx_id`, so the count sat stale until a reload. Caught in browser testing, not by
  the tests. Fixed by extending `getPostCounts` — the existing "Live boot counts" channel —
  to return `reply_count` too, in the same request. A short-lived `getReplyCounts` server
  action written earlier the same day was DELETED as dead code once the join superseded it.

- **Live counts are no longer gated on `tx_id`.** `useFeedPolling` used to request
  authoritative counts only for posts that had already anchored on-chain. A post is
  replyable and bootable the instant it exists in the DB, well before it anchors — under
  that filter a fresh post's counts stayed frozen until its OP_RETURN landed, and stayed
  frozen forever wherever `BSV_SERVER_WIF` is unset (local dev, or a paused wallet, which is
  how the bug surfaced). The id list is capped at 100 either way, so covering all visible
  posts costs nothing extra. This also fixes the same latent staleness for boot counts.

- **Open question, carried from TOKENS.md and NOT settled here:** does ordinary posting become
  paid? Paying to *mint a ticker* is a founding act and a natural fee. Paying to *post* is a
  different thing and would end the zero-friction onboarding DIRECTION.md's conversion claim
  rests on. Separate these explicitly before either is built.
